import { o as __toESM, t as __commonJSMin } from "./chunk-BmQy1IqS.js";
import { t as require_react } from "./react.js";
import { Br as RpcRequestError, Gr as ContractFunctionExecutionError, Ka as fromHex, Lr as UserRejectedRequestError, Oa as keccak256, Ta as getAddress, Ua as numberToHex, Wa as stringToHex, Ya as hexToNumber, kr as SwitchChainError, pr as ChainDisconnectedError, u as call$1, wr as ProviderDisconnectedError } from "./secp256k1-DR4_-BNn.js";
import { $t as getBlock$1, Pt as getBlockNumber$1, Qt as getGasPrice$1, Xt as estimateFeesPerGas$1, Yt as getTransactionCount$1, Zt as estimateMaxPriorityFeePerGas$1, b as getTransaction$1, hn as readContract$1, kt as getCode, r as sendTransactionSync$1, t as writeContractSync$1, u as watchBlockNumber$1, y as getTransactionReceipt$1, yt as withRetry } from "./writeContractSync-Kspj32L_.js";
import { $ as getTransactionConfirmations$1, A as watchAsset$1, At as getFeeHistory$1, B as getCapabilities$1, Bt as getEnsText$1, E as publicActions, F as sendCallsSync$1, G as watchContractEvent$1, Gt as getEnsAddress$1, H as sendCalls$1, J as verifyMessage$1, K as watchBlocks$1, L as waitForCallsStatus$1, Lt as getEnsResolver$1, M as signTransaction$1, Mt as getBlockTransactionCount$1, Nt as getBlobBaseFee$1, P as showCallsStatus$1, Q as multicall$1, Rt as getEnsName$1, S as walletActions, U as deployContract$1, V as getCallsStatus$1, Y as simulateContract$1, _ as fallback$1, b as createTransport, et as getStorageAt$1, h as http, j as signTypedData$1, jt as getContractEvents$1, m as webSocket, q as verifyTypedData$1, tt as getProof$1, vt as rpc, y as custom, yt as withTimeout, zt as getEnsAvatar$1 } from "./_esm-hbsOY9FW.js";
import { A as ChainNotConfiguredError, C as getConnection, D as disconnect, E as estimateGas, F as ConnectorNotFoundError, I as ConnectorUnavailableReconnectingError, L as BaseError$1, M as ConnectorAlreadyConnectedError, N as ConnectorChainMismatchError, O as getConnectorClient, P as ConnectorNotConnectedError, R as getAction, S as getConnections, T as getBalance, _ as SwitchChainNotSupportedError, a as deserialize, b as reconnect, c as createConnector, d as watchConnectors, f as watchConnections, g as ProviderNotFoundError, h as switchChain, i as serialize, j as ConnectorAccountNotFoundError, k as connect, l as writeContract, m as waitForTransactionReceipt, n as createStorage, p as watchConnection, r as noopStorage, s as injected, t as createConfig, u as watchPendingTransactions, v as signMessage, w as deepEqual, x as prepareTransactionRequest, y as sendTransaction } from "./createConfig-CGDjXFfS.js";
import { C as useQueryClient, et as replaceEqualDeep, h as useQuery$1, r as useMutation, t as useInfiniteQuery$1 } from "./modern-CJT3Tayd.js";
//#region node_modules/@wagmi/core/dist/esm/actions/call.js
async function call(config, parameters) {
	const { chainId, ...rest } = parameters;
	return getAction(config.getClient({ chainId }), call$1, "call")(rest);
}
//#endregion
//#region node_modules/@wagmi/core/dist/esm/actions/deployContract.js
/** https://wagmi.sh/core/api/actions/deployContract */
async function deployContract(config, parameters) {
	const { account, chainId, connector, ...rest } = parameters;
	let client;
	if (typeof account === "object" && account?.type === "local") client = config.getClient({ chainId });
	else client = await getConnectorClient(config, {
		account: account ?? void 0,
		assertChainId: false,
		chainId,
		connector
	});
	const chain = (() => {
		if (!chainId || client.chain?.id === chainId) return client.chain;
		return { id: chainId };
	})();
	return await getAction(client, deployContract$1, "deployContract")({
		...rest,
		...account ? { account } : {},
		assertChainId: !!chainId,
		chain
	});
}
//#endregion
//#region node_modules/@wagmi/core/dist/esm/actions/estimateFeesPerGas.js
async function estimateFeesPerGas(config, parameters = {}) {
	const { chainId, ...rest } = parameters;
	const client = config.getClient({ chainId });
	const { gasPrice, maxFeePerGas, maxPriorityFeePerGas } = await getAction(client, estimateFeesPerGas$1, "estimateFeesPerGas")({
		...rest,
		chain: client.chain
	});
	return {
		gasPrice,
		maxFeePerGas,
		maxPriorityFeePerGas
	};
}
//#endregion
//#region node_modules/@wagmi/core/dist/esm/actions/estimateMaxPriorityFeePerGas.js
/** https://wagmi.sh/core/api/actions/estimateMaxPriorityFeePerGas */
async function estimateMaxPriorityFeePerGas(config, parameters = {}) {
	const { chainId } = parameters;
	const client = config.getClient({ chainId });
	return getAction(client, estimateMaxPriorityFeePerGas$1, "estimateMaxPriorityFeePerGas")({ chain: client.chain });
}
//#endregion
//#region node_modules/@wagmi/core/dist/esm/actions/getBlobBaseFee.js
/** https://wagmi.sh/core/api/actions/getBlobBaseFee */
function getBlobBaseFee(config, parameters = {}) {
	const { chainId } = parameters;
	return getAction(config.getClient({ chainId }), getBlobBaseFee$1, "getBlobBaseFee")({});
}
//#endregion
//#region node_modules/@wagmi/core/dist/esm/actions/getBlock.js
/** https://wagmi.sh/core/actions/getBlock */
async function getBlock(config, parameters = {}) {
	const { chainId, ...rest } = parameters;
	const client = config.getClient({ chainId });
	return {
		...await getAction(client, getBlock$1, "getBlock")(rest),
		chainId: client.chain.id
	};
}
//#endregion
//#region node_modules/@wagmi/core/dist/esm/actions/getBlockNumber.js
/** https://wagmi.sh/core/api/actions/getBlockNumber */
function getBlockNumber(config, parameters = {}) {
	const { chainId, ...rest } = parameters;
	return getAction(config.getClient({ chainId }), getBlockNumber$1, "getBlockNumber")(rest);
}
//#endregion
//#region node_modules/@wagmi/core/dist/esm/actions/getBlockTransactionCount.js
/** https://wagmi.sh/core/api/actions/getBlockTransactionCount */
function getBlockTransactionCount(config, parameters = {}) {
	const { chainId, ...rest } = parameters;
	return getAction(config.getClient({ chainId }), getBlockTransactionCount$1, "getBlockTransactionCount")(rest);
}
//#endregion
//#region node_modules/@wagmi/core/dist/esm/actions/getBytecode.js
/** https://wagmi.sh/core/api/actions/getBytecode */
async function getBytecode(config, parameters) {
	const { chainId, ...rest } = parameters;
	return getAction(config.getClient({ chainId }), getCode, "getBytecode")(rest);
}
//#endregion
//#region node_modules/@wagmi/core/dist/esm/actions/getCallsStatus.js
/** https://wagmi.sh/core/api/actions/getCallsStatus */
async function getCallsStatus(config, parameters) {
	const { connector, id } = parameters;
	return getAction(await getConnectorClient(config, { connector }), getCallsStatus$1, "getCallsStatus")({ id });
}
//#endregion
//#region node_modules/@wagmi/core/dist/esm/actions/getCapabilities.js
/** https://wagmi.sh/core/api/actions/getCapabilities */
async function getCapabilities(config, parameters = {}) {
	const { account, chainId, connector } = parameters;
	return getCapabilities$1(await getConnectorClient(config, {
		account,
		connector
	}), {
		account,
		chainId
	});
}
//#endregion
//#region node_modules/@wagmi/core/dist/esm/actions/getChainId.js
/** https://wagmi.sh/core/api/actions/getChainId */
function getChainId(config) {
	return config.state.chainId;
}
//#endregion
//#region node_modules/@wagmi/core/dist/esm/actions/getChains.js
var previousChains = [];
/** https://wagmi.sh/core/api/actions/getChains */
function getChains(config) {
	const chains = config.chains;
	if (deepEqual(previousChains, chains)) return previousChains;
	previousChains = chains;
	return chains;
}
//#endregion
//#region node_modules/@wagmi/core/dist/esm/actions/getClient.js
function getClient(config, parameters = {}) {
	try {
		return config.getClient(parameters);
	} catch {
		return;
	}
}
//#endregion
//#region node_modules/@wagmi/core/dist/esm/actions/getConnectors.js
var previousConnectors = [];
/** https://wagmi.sh/core/api/actions/getConnectors */
function getConnectors(config) {
	const connectors = config.connectors;
	if (previousConnectors.length === connectors.length && previousConnectors.every((connector, index) => connector === connectors[index])) return previousConnectors;
	previousConnectors = connectors;
	return connectors;
}
//#endregion
//#region node_modules/@wagmi/core/dist/esm/actions/getContractEvents.js
/** https://wagmi.sh/core/actions/getContractEvents */
async function getContractEvents(config, parameters) {
	const { chainId, ...rest } = parameters;
	return getAction(config.getClient({ chainId }), getContractEvents$1, "getContractEvents")(rest);
}
//#endregion
//#region node_modules/@wagmi/core/dist/esm/actions/getEnsAddress.js
/** https://wagmi.sh/core/api/actions/getEnsAddress */
function getEnsAddress(config, parameters) {
	const { chainId, ...rest } = parameters;
	return getAction(config.getClient({ chainId }), getEnsAddress$1, "getEnsAddress")(rest);
}
//#endregion
//#region node_modules/@wagmi/core/dist/esm/actions/getEnsAvatar.js
/** https://wagmi.sh/core/api/actions/getEnsAvatar */
function getEnsAvatar(config, parameters) {
	const { chainId, ...rest } = parameters;
	return getAction(config.getClient({ chainId }), getEnsAvatar$1, "getEnsAvatar")(rest);
}
//#endregion
//#region node_modules/@wagmi/core/dist/esm/actions/getEnsName.js
/** https://wagmi.sh/core/api/actions/getEnsName */
function getEnsName(config, parameters) {
	const { chainId, ...rest } = parameters;
	return getAction(config.getClient({ chainId }), getEnsName$1, "getEnsName")(rest);
}
//#endregion
//#region node_modules/@wagmi/core/dist/esm/actions/getEnsResolver.js
/** https://wagmi.sh/core/api/actions/getEnsResolver */
function getEnsResolver(config, parameters) {
	const { chainId, ...rest } = parameters;
	return getAction(config.getClient({ chainId }), getEnsResolver$1, "getEnsResolver")(rest);
}
//#endregion
//#region node_modules/@wagmi/core/dist/esm/actions/getEnsText.js
/** https://wagmi.sh/core/api/actions/getEnsText */
function getEnsText(config, parameters) {
	const { chainId, ...rest } = parameters;
	return getAction(config.getClient({ chainId }), getEnsText$1, "getEnsText")(rest);
}
//#endregion
//#region node_modules/@wagmi/core/dist/esm/actions/getFeeHistory.js
/** https://wagmi.sh/core/api/actions/getFeeHistory */
function getFeeHistory(config, parameters) {
	const { chainId, ...rest } = parameters;
	return getAction(config.getClient({ chainId }), getFeeHistory$1, "getFeeHistory")(rest);
}
//#endregion
//#region node_modules/@wagmi/core/dist/esm/actions/getGasPrice.js
/** https://wagmi.sh/core/api/actions/getGasPrice */
function getGasPrice(config, parameters = {}) {
	const { chainId } = parameters;
	return getAction(config.getClient({ chainId }), getGasPrice$1, "getGasPrice")({});
}
//#endregion
//#region node_modules/@wagmi/core/dist/esm/actions/getProof.js
/** https://wagmi.sh/core/api/actions/getProof */
async function getProof(config, parameters) {
	const { chainId, ...rest } = parameters;
	return getAction(config.getClient({ chainId }), getProof$1, "getProof")(rest);
}
//#endregion
//#region node_modules/@wagmi/core/dist/esm/actions/getPublicClient.js
function getPublicClient(config, parameters = {}) {
	return getClient(config, parameters)?.extend(publicActions);
}
//#endregion
//#region node_modules/@wagmi/core/dist/esm/actions/getStorageAt.js
/** https://wagmi.sh/core/api/actions/getStorageAt */
async function getStorageAt(config, parameters) {
	const { chainId, ...rest } = parameters;
	return getAction(config.getClient({ chainId }), getStorageAt$1, "getStorageAt")(rest);
}
//#endregion
//#region node_modules/@wagmi/core/dist/esm/actions/getTransaction.js
/** https://wagmi.sh/core/api/actions/getTransaction */
function getTransaction(config, parameters) {
	const { chainId, ...rest } = parameters;
	return getAction(config.getClient({ chainId }), getTransaction$1, "getTransaction")(rest);
}
//#endregion
//#region node_modules/@wagmi/core/dist/esm/actions/getTransactionConfirmations.js
/** https://wagmi.sh/core/api/actions/getTransactionConfirmations */
function getTransactionConfirmations(config, parameters) {
	const { chainId, ...rest } = parameters;
	return getAction(config.getClient({ chainId }), getTransactionConfirmations$1, "getTransactionConfirmations")(rest);
}
//#endregion
//#region node_modules/@wagmi/core/dist/esm/actions/getTransactionCount.js
/** https://wagmi.sh/core/api/actions/getTransactionCount */
async function getTransactionCount(config, parameters) {
	const { address, blockNumber, blockTag, chainId } = parameters;
	return getAction(config.getClient({ chainId }), getTransactionCount$1, "getTransactionCount")(blockNumber ? {
		address,
		blockNumber
	} : {
		address,
		blockTag
	});
}
//#endregion
//#region node_modules/@wagmi/core/dist/esm/actions/getTransactionReceipt.js
/** https://wagmi.sh/core/api/actions/getTransactionReceipt */
async function getTransactionReceipt(config, parameters) {
	const { chainId, ...rest } = parameters;
	return getAction(config.getClient({ chainId }), getTransactionReceipt$1, "getTransactionReceipt")(rest);
}
//#endregion
//#region node_modules/@wagmi/core/dist/esm/actions/getWalletClient.js
async function getWalletClient(config, parameters = {}) {
	return (await getConnectorClient(config, parameters)).extend(walletActions);
}
//#endregion
//#region node_modules/@wagmi/core/dist/esm/actions/multicall.js
async function multicall(config, parameters) {
	const { allowFailure = true, chainId, contracts, ...rest } = parameters;
	return getAction(config.getClient({ chainId }), multicall$1, "multicall")({
		allowFailure,
		contracts,
		...rest
	});
}
//#endregion
//#region node_modules/@wagmi/core/dist/esm/actions/readContract.js
/** https://wagmi.sh/core/api/actions/readContract */
function readContract(config, parameters) {
	const { chainId, ...rest } = parameters;
	return getAction(config.getClient({ chainId }), readContract$1, "readContract")(rest);
}
//#endregion
//#region node_modules/@wagmi/core/dist/esm/actions/readContracts.js
async function readContracts(config, parameters) {
	const { allowFailure = true, blockNumber, blockTag, ...rest } = parameters;
	const contracts = parameters.contracts;
	try {
		const contractsByChainId = {};
		for (const [index, contract] of contracts.entries()) {
			const chainId = contract.chainId ?? config.state.chainId;
			if (!contractsByChainId[chainId]) contractsByChainId[chainId] = [];
			contractsByChainId[chainId]?.push({
				contract,
				index
			});
		}
		const promises = () => Object.entries(contractsByChainId).map(([chainId, contracts]) => multicall(config, {
			...rest,
			allowFailure,
			blockNumber,
			blockTag,
			chainId: Number.parseInt(chainId, 10),
			contracts: contracts.map(({ contract }) => contract)
		}));
		const multicallResults = (await Promise.all(promises())).flat();
		const resultIndexes = Object.values(contractsByChainId).flatMap((contracts) => contracts.map(({ index }) => index));
		return multicallResults.reduce((results, result, index) => {
			if (results) results[resultIndexes[index]] = result;
			return results;
		}, []);
	} catch (error) {
		if (error instanceof ContractFunctionExecutionError) throw error;
		const promises = () => contracts.map((contract) => readContract(config, {
			...contract,
			blockNumber,
			blockTag
		}));
		if (allowFailure) return (await Promise.allSettled(promises())).map((result) => {
			if (result.status === "fulfilled") return {
				result: result.value,
				status: "success"
			};
			return {
				error: result.reason,
				result: void 0,
				status: "failure"
			};
		});
		return await Promise.all(promises());
	}
}
//#endregion
//#region node_modules/@wagmi/core/dist/esm/actions/sendCalls.js
/** https://wagmi.sh/core/api/actions/sendCalls */
async function sendCalls(config, parameters) {
	const { account, chainId, connector, calls, ...rest } = parameters;
	return getAction(await getConnectorClient(config, {
		account,
		assertChainId: false,
		chainId,
		connector
	}), sendCalls$1, "sendCalls")({
		...rest,
		...typeof account !== "undefined" ? { account } : {},
		calls,
		chain: chainId ? { id: chainId } : void 0
	});
}
//#endregion
//#region node_modules/@wagmi/core/dist/esm/actions/sendCallsSync.js
/** https://wagmi.sh/core/api/actions/sendCallsSync */
async function sendCallsSync(config, parameters) {
	const { account, chainId, connector, calls, ...rest } = parameters;
	return getAction(await getConnectorClient(config, {
		account,
		assertChainId: false,
		chainId,
		connector
	}), sendCallsSync$1, "sendCallsSync")({
		...rest,
		...typeof account !== "undefined" ? { account } : {},
		calls,
		chain: chainId ? { id: chainId } : void 0
	});
}
//#endregion
//#region node_modules/@wagmi/core/dist/esm/actions/sendTransactionSync.js
/** https://wagmi.sh/core/api/actions/sendTransactionSync */
async function sendTransactionSync(config, parameters) {
	const { account, chainId, connector, ...rest } = parameters;
	let client;
	if (typeof account === "object" && account?.type === "local") client = config.getClient({ chainId });
	else client = await getConnectorClient(config, {
		account: account ?? void 0,
		assertChainId: false,
		chainId,
		connector
	});
	const chain = (() => {
		if (!chainId || client.chain?.id === chainId) return client.chain;
		return { id: chainId };
	})();
	return await getAction(client, sendTransactionSync$1, "sendTransactionSync")({
		...rest,
		...account ? { account } : {},
		assertChainId: !!chainId,
		chain,
		gas: rest.gas ?? void 0
	});
}
//#endregion
//#region node_modules/@wagmi/core/dist/esm/actions/showCallsStatus.js
/** https://wagmi.sh/core/api/actions/showCallsStatus */
async function showCallsStatus(config, parameters) {
	const { connector, id } = parameters;
	return showCallsStatus$1(await getConnectorClient(config, { connector }), { id });
}
//#endregion
//#region node_modules/@wagmi/core/dist/esm/actions/signTransaction.js
/** https://wagmi.sh/core/api/actions/signTransaction */
async function signTransaction(config, parameters) {
	const { account, chainId, connector, ...rest } = parameters;
	let client;
	if (typeof account === "object" && account?.type === "local") client = config.getClient({ chainId });
	else client = await getConnectorClient(config, {
		account: account ?? void 0,
		assertChainId: false,
		chainId,
		connector
	});
	const chain = (() => {
		if (!chainId || client.chain?.id === chainId) return client.chain;
		return { id: chainId };
	})();
	return await getAction(client, signTransaction$1, "signTransaction")({
		...rest,
		...account ? { account } : {},
		assertChainId: !!chainId,
		chain,
		gas: rest.gas ?? void 0
	});
}
//#endregion
//#region node_modules/@wagmi/core/dist/esm/actions/signTypedData.js
/** https://wagmi.sh/core/api/actions/signTypedData */
async function signTypedData(config, parameters) {
	const { account, connector, ...rest } = parameters;
	let client;
	if (typeof account === "object" && account.type === "local") client = config.getClient();
	else client = await getConnectorClient(config, {
		account,
		connector
	});
	return getAction(client, signTypedData$1, "signTypedData")({
		...rest,
		...account ? { account } : {}
	});
}
//#endregion
//#region node_modules/@wagmi/core/dist/esm/actions/simulateContract.js
/** https://wagmi.sh/core/api/actions/simulateContract */
async function simulateContract(config, parameters) {
	const { abi, chainId, connector, ...rest } = parameters;
	let account;
	if (parameters.account) account = parameters.account;
	else account = (await getConnectorClient(config, {
		assertChainId: false,
		chainId,
		connector
	})).account;
	const client = config.getClient({ chainId });
	const { result, request } = await getAction(client, simulateContract$1, "simulateContract")({
		...rest,
		abi,
		account
	});
	return {
		chainId: client.chain.id,
		result,
		request: {
			...request,
			chainId
		}
	};
}
//#endregion
//#region node_modules/@wagmi/core/dist/esm/actions/switchConnection.js
/** https://wagmi.sh/core/api/actions/switchConnection */
async function switchConnection(config, parameters) {
	const { connector } = parameters;
	const connection = config.state.connections.get(connector.uid);
	if (!connection) throw new ConnectorNotConnectedError();
	await config.storage?.setItem("recentConnectorId", connector.id);
	config.setState((x) => ({
		...x,
		current: connector.uid
	}));
	return {
		accounts: connection.accounts,
		chainId: connection.chainId
	};
}
//#endregion
//#region node_modules/@wagmi/core/dist/esm/actions/verifyMessage.js
/** https://wagmi.sh/core/api/actions/verifyMessage */
async function verifyMessage(config, parameters) {
	const { chainId, ...rest } = parameters;
	return getAction(config.getClient({ chainId }), verifyMessage$1, "verifyMessage")(rest);
}
//#endregion
//#region node_modules/@wagmi/core/dist/esm/actions/verifyTypedData.js
/** https://wagmi.sh/core/api/actions/verifyTypedData */
async function verifyTypedData(config, parameters) {
	const { chainId, ...rest } = parameters;
	return getAction(config.getClient({ chainId }), verifyTypedData$1, "verifyTypedData")(rest);
}
//#endregion
//#region node_modules/@wagmi/core/dist/esm/actions/waitForCallsStatus.js
/** https://wagmi.sh/core/api/actions/waitForCallsStatus */
async function waitForCallsStatus(config, parameters) {
	const { connector } = parameters;
	return waitForCallsStatus$1(await getConnectorClient(config, { connector }), parameters);
}
//#endregion
//#region node_modules/@wagmi/core/dist/esm/actions/watchAsset.js
/** https://wagmi.sh/core/api/actions/watchAsset */
async function watchAsset(config, parameters) {
	const { connector, ...rest } = parameters;
	return getAction(await getConnectorClient(config, { connector }), watchAsset$1, "watchAsset")(rest);
}
//#endregion
//#region node_modules/@wagmi/core/dist/esm/actions/watchBlockNumber.js
/** https://wagmi.sh/core/api/actions/watchBlockNumber */
function watchBlockNumber(config, parameters) {
	const { syncConnectedChain = config._internal.syncConnectedChain, ...rest } = parameters;
	let unwatch;
	const listener = (chainId) => {
		if (unwatch) unwatch();
		unwatch = getAction(config.getClient({ chainId }), watchBlockNumber$1, "watchBlockNumber")(rest);
		return unwatch;
	};
	const unlisten = listener(parameters.chainId);
	let unsubscribe;
	if (syncConnectedChain && !parameters.chainId) unsubscribe = config.subscribe(({ chainId }) => chainId, async (chainId) => listener(chainId));
	return () => {
		unlisten?.();
		unsubscribe?.();
	};
}
//#endregion
//#region node_modules/@wagmi/core/dist/esm/actions/watchBlocks.js
/** https://wagmi.sh/core/actions/watchBlocks */
function watchBlocks(config, parameters) {
	const { syncConnectedChain = config._internal.syncConnectedChain, ...rest } = parameters;
	let unwatch;
	const listener = (chainId) => {
		if (unwatch) unwatch();
		unwatch = getAction(config.getClient({ chainId }), watchBlocks$1, "watchBlocks")(rest);
		return unwatch;
	};
	const unlisten = listener(parameters.chainId);
	let unsubscribe;
	if (syncConnectedChain && !parameters.chainId) unsubscribe = config.subscribe(({ chainId }) => chainId, async (chainId) => listener(chainId));
	return () => {
		unlisten?.();
		unsubscribe?.();
	};
}
//#endregion
//#region node_modules/@wagmi/core/dist/esm/actions/watchChainId.js
/** https://wagmi.sh/core/api/actions/watchChainId */
function watchChainId(config, parameters) {
	const { onChange } = parameters;
	return config.subscribe((state) => state.chainId, onChange);
}
//#endregion
//#region node_modules/@wagmi/core/dist/esm/actions/watchClient.js
/** https://wagmi.sh/core/api/actions/watchClient */
function watchClient(config, parameters) {
	const { onChange } = parameters;
	return config.subscribe(() => getClient(config), onChange, { equalityFn(a, b) {
		return a?.uid === b?.uid;
	} });
}
//#endregion
//#region node_modules/@wagmi/core/dist/esm/actions/watchContractEvent.js
/** https://wagmi.sh/core/api/actions/watchContractEvent */
function watchContractEvent(config, parameters) {
	const { syncConnectedChain = config._internal.syncConnectedChain, ...rest } = parameters;
	let unwatch;
	const listener = (chainId) => {
		if (unwatch) unwatch();
		unwatch = getAction(config.getClient({ chainId }), watchContractEvent$1, "watchContractEvent")(rest);
		return unwatch;
	};
	const unlisten = listener(parameters.chainId);
	let unsubscribe;
	if (syncConnectedChain && !parameters.chainId) unsubscribe = config.subscribe(({ chainId }) => chainId, async (chainId) => listener(chainId));
	return () => {
		unlisten?.();
		unsubscribe?.();
	};
}
//#endregion
//#region node_modules/@wagmi/core/dist/esm/actions/watchPublicClient.js
/** https://wagmi.sh/core/api/actions/watchPublicClient */
function watchPublicClient(config, parameters) {
	const { onChange } = parameters;
	return config.subscribe(() => getPublicClient(config), onChange, { equalityFn(a, b) {
		return a?.uid === b?.uid;
	} });
}
//#endregion
//#region node_modules/@wagmi/core/dist/esm/actions/writeContractSync.js
/** https://wagmi.sh/core/api/actions/writeContractSync */
async function writeContractSync(config, parameters) {
	const { account, chainId, connector, ...request } = parameters;
	let client;
	if (typeof account === "object" && account?.type === "local") client = config.getClient({ chainId });
	else client = await getConnectorClient(config, {
		account: account ?? void 0,
		assertChainId: false,
		chainId,
		connector
	});
	const chain = (() => {
		if (!chainId || client.chain?.id === chainId) return client.chain;
		return { id: chainId };
	})();
	return await getAction(client, writeContractSync$1, "writeContractSync")({
		...request,
		...account ? { account } : {},
		assertChainId: !!chainId,
		chain
	});
}
//#endregion
//#region node_modules/@wagmi/core/dist/esm/connectors/mock.js
mock.type = "mock";
function mock(parameters) {
	const transactionCache = /* @__PURE__ */ new Map();
	const features = parameters.features ?? { defaultConnected: false };
	let connected = features.defaultConnected;
	let connectedChainId;
	return createConnector((config) => ({
		id: "mock",
		name: "Mock Connector",
		type: mock.type,
		async setup() {
			connectedChainId = config.chains[0].id;
		},
		async connect({ chainId, withCapabilities } = {}) {
			if (features.connectError) {
				if (typeof features.connectError === "boolean") throw new UserRejectedRequestError(/* @__PURE__ */ new Error("Failed to connect."));
				throw features.connectError;
			}
			const accounts = await (await this.getProvider()).request({ method: "eth_requestAccounts" });
			let currentChainId = await this.getChainId();
			if (chainId && currentChainId !== chainId) currentChainId = (await this.switchChain({ chainId })).id;
			connected = true;
			return {
				accounts: withCapabilities ? accounts.map((x) => ({
					address: getAddress(x),
					capabilities: { foo: { bar: x } }
				})) : accounts.map((x) => getAddress(x)),
				chainId: currentChainId
			};
		},
		async disconnect() {
			connected = false;
		},
		async getAccounts() {
			if (!connected) throw new ConnectorNotConnectedError();
			return (await (await this.getProvider()).request({ method: "eth_accounts" })).map((x) => getAddress(x));
		},
		async getChainId() {
			return fromHex(await (await this.getProvider()).request({ method: "eth_chainId" }), "number");
		},
		async isAuthorized() {
			if (!features.reconnect) return false;
			if (!connected) return false;
			return !!(await this.getAccounts()).length;
		},
		async switchChain({ chainId }) {
			const provider = await this.getProvider();
			const chain = config.chains.find((x) => x.id === chainId);
			if (!chain) throw new SwitchChainError(new ChainNotConfiguredError());
			await provider.request({
				method: "wallet_switchEthereumChain",
				params: [{ chainId: numberToHex(chainId) }]
			});
			return chain;
		},
		onAccountsChanged(accounts) {
			if (accounts.length === 0) this.onDisconnect();
			else config.emitter.emit("change", { accounts: accounts.map((x) => getAddress(x)) });
		},
		onChainChanged(chain) {
			const chainId = Number(chain);
			config.emitter.emit("change", { chainId });
		},
		async onDisconnect(_error) {
			config.emitter.emit("disconnect");
			connected = false;
		},
		async getProvider({ chainId } = {}) {
			const url = (config.chains.find((x) => x.id === chainId) ?? config.chains[0]).rpcUrls.default.http[0];
			const request = async ({ method, params }) => {
				if (method === "eth_chainId") return numberToHex(connectedChainId);
				if (method === "eth_accounts") return parameters.accounts;
				if (method === "eth_requestAccounts") return parameters.accounts;
				if (method === "eth_signTypedData_v4") {
					if (features.signTypedDataError) {
						if (typeof features.signTypedDataError === "boolean") throw new UserRejectedRequestError(/* @__PURE__ */ new Error("Failed to sign typed data."));
						throw features.signTypedDataError;
					}
				}
				if (method === "wallet_switchEthereumChain") {
					if (features.switchChainError) {
						if (typeof features.switchChainError === "boolean") throw new UserRejectedRequestError(/* @__PURE__ */ new Error("Failed to switch chain."));
						throw features.switchChainError;
					}
					connectedChainId = fromHex(params[0].chainId, "number");
					this.onChainChanged(connectedChainId.toString());
					return;
				}
				if (method === "wallet_watchAsset") {
					if (features.watchAssetError) {
						if (typeof features.watchAssetError === "boolean") throw new UserRejectedRequestError(/* @__PURE__ */ new Error("Failed to switch chain."));
						throw features.watchAssetError;
					}
					return connected;
				}
				if (method === "wallet_getCapabilities") return {
					"0x2105": {
						paymasterService: { supported: params[0] === "0x95132632579b073D12a6673e18Ab05777a6B86f8" },
						sessionKeys: { supported: true }
					},
					"0x14A34": { paymasterService: { supported: params[0] === "0x95132632579b073D12a6673e18Ab05777a6B86f8" } }
				};
				if (method === "wallet_sendCalls") {
					const hashes = [];
					const calls = params[0].calls;
					const from = params[0].from;
					for (const call of calls) {
						const { result, error } = await rpc.http(url, { body: {
							method: "eth_sendTransaction",
							params: [{
								...call,
								...typeof from !== "undefined" ? { from } : {}
							}]
						} });
						if (error) throw new RpcRequestError({
							body: {
								method,
								params
							},
							error,
							url
						});
						hashes.push(result);
					}
					const id = keccak256(stringToHex(JSON.stringify(calls)));
					transactionCache.set(id, hashes);
					return { id };
				}
				if (method === "wallet_getCallsStatus") {
					const hashes = transactionCache.get(params[0]);
					if (!hashes) return {
						atomic: false,
						chainId: "0x1",
						id: params[0],
						status: 100,
						receipts: [],
						version: "2.0.0"
					};
					const receipts_ = (await Promise.all(hashes.map(async (hash) => {
						const { result, error } = await rpc.http(url, { body: {
							method: "eth_getTransactionReceipt",
							params: [hash],
							id: 0
						} });
						if (error) throw new RpcRequestError({
							body: {
								method,
								params
							},
							error,
							url
						});
						if (!result) return null;
						return {
							blockHash: result.blockHash,
							blockNumber: result.blockNumber,
							gasUsed: result.gasUsed,
							logs: result.logs,
							status: result.status,
							transactionHash: result.transactionHash
						};
					}))).filter((x) => x !== null);
					if (receipts_.length === 0) return {
						atomic: false,
						chainId: "0x1",
						id: params[0],
						status: 100,
						receipts: [],
						version: "2.0.0"
					};
					return {
						atomic: false,
						chainId: "0x1",
						id: params[0],
						status: 200,
						receipts: receipts_,
						version: "2.0.0"
					};
				}
				if (method === "wallet_showCallsStatus") return;
				if (method === "personal_sign") {
					if (features.signMessageError) {
						if (typeof features.signMessageError === "boolean") throw new UserRejectedRequestError(/* @__PURE__ */ new Error("Failed to sign message."));
						throw features.signMessageError;
					}
					method = "eth_sign";
					params = [params[1], params[0]];
				}
				const body = {
					method,
					params
				};
				const { error, result } = await rpc.http(url, { body });
				if (error) throw new RpcRequestError({
					body,
					error,
					url
				});
				return result;
			};
			return custom({ request })({ retryCount: 0 });
		}
	}));
}
//#endregion
//#region node_modules/@wagmi/core/dist/esm/hydrate.js
function hydrate(config, parameters) {
	const { initialState, reconnectOnMount } = parameters;
	if (initialState && !config._internal.store.persist.hasHydrated()) config.setState({
		...initialState,
		chainId: config.chains.some((x) => x.id === initialState.chainId) ? initialState.chainId : config.chains[0].id,
		connections: reconnectOnMount ? initialState.connections : /* @__PURE__ */ new Map(),
		status: reconnectOnMount ? "reconnecting" : "disconnected"
	});
	return { async onMount() {
		if (config._internal.ssr) {
			await config._internal.store.persist.rehydrate();
			if (config._internal.mipd) config._internal.connectors.setState((connectors) => {
				const rdnsSet = /* @__PURE__ */ new Set();
				for (const connector of connectors ?? []) if (connector.rdns) {
					const rdnsValues = Array.isArray(connector.rdns) ? connector.rdns : [connector.rdns];
					for (const rdns of rdnsValues) rdnsSet.add(rdns);
				}
				const mipdConnectors = [];
				const providers = config._internal.mipd?.getProviders() ?? [];
				for (const provider of providers) {
					if (rdnsSet.has(provider.info.rdns)) continue;
					const connectorFn = config._internal.connectors.providerDetailToConnector(provider);
					const connector = config._internal.connectors.setup(connectorFn);
					mipdConnectors.push(connector);
				}
				return [...connectors, ...mipdConnectors];
			});
		}
		if (reconnectOnMount) reconnect(config);
		else if (config.storage) config.setState((x) => ({
			...x,
			connections: /* @__PURE__ */ new Map()
		}));
	} };
}
//#endregion
//#region node_modules/@wagmi/core/dist/esm/transports/connector.js
function unstable_connector(connector, config = {}) {
	const { type } = connector;
	const { key = "connector", name = "Connector", retryDelay } = config;
	return (parameters) => {
		const { chain, connectors } = parameters;
		const retryCount = config.retryCount ?? parameters.retryCount;
		const request = async ({ method, params }) => {
			const connector = connectors?.getState().find((c) => c.type === type);
			if (!connector) throw new ProviderDisconnectedError(/* @__PURE__ */ new Error(`Could not find connector of type "${type}" in \`connectors\` passed to \`createConfig\`.`));
			const provider = await connector.getProvider({ chainId: chain?.id });
			if (!provider) throw new ProviderDisconnectedError(/* @__PURE__ */ new Error("Provider is disconnected."));
			const chainId = hexToNumber(await withRetry(() => withTimeout(() => provider.request({ method: "eth_chainId" }), { timeout: 100 })));
			if (chain && chainId !== chain.id) throw new ChainDisconnectedError(/* @__PURE__ */ new Error(`The current chain of the connector (id: ${chainId}) does not match the target chain for the request (id: ${chain.id} – ${chain.name}).`));
			const body = {
				method,
				params
			};
			return provider.request(body);
		};
		return createTransport({
			key,
			name,
			request,
			retryCount,
			retryDelay,
			type: "connector"
		});
	};
}
//#endregion
//#region node_modules/@wagmi/core/dist/esm/transports/fallback.js
function fallback(transports, config) {
	return fallback$1(transports, config);
}
//#endregion
//#region node_modules/@wagmi/core/dist/esm/utils/cookie.js
var cookieStorage = {
	getItem(key) {
		if (typeof window === "undefined") return null;
		return parseCookie(document.cookie, key) ?? null;
	},
	setItem(key, value) {
		if (typeof window === "undefined") return;
		document.cookie = `${key}=${value};path=/;samesite=Lax`;
	},
	removeItem(key) {
		if (typeof window === "undefined") return;
		document.cookie = `${key}=;max-age=-1;path=/`;
	}
};
function cookieToInitialState(config, cookie) {
	if (!cookie) return void 0;
	const parsed = parseCookie(cookie, `${config.storage?.key}.store`);
	if (!parsed) return void 0;
	try {
		return deserialize(parsed)?.state;
	} catch {
		return;
	}
}
function parseCookie(cookie, key) {
	const keyValue = cookie.split("; ").find((x) => x.startsWith(`${key}=`));
	if (!keyValue) return void 0;
	return keyValue.substring(key.length + 1);
}
//#endregion
//#region node_modules/wagmi/dist/esm/hydrate.js
var import_react = /* @__PURE__ */ __toESM(require_react(), 1);
function Hydrate(parameters) {
	const { children, config, initialState, reconnectOnMount = true } = parameters;
	const { onMount } = hydrate(config, {
		initialState,
		reconnectOnMount
	});
	if (!config._internal.ssr) onMount();
	const active = (0, import_react.useRef)(true);
	(0, import_react.useEffect)(() => {
		if (!active.current) return;
		if (!config._internal.ssr) return;
		onMount();
		return () => {
			active.current = false;
		};
	}, []);
	return children;
}
//#endregion
//#region node_modules/wagmi/dist/esm/context.js
var WagmiContext = (0, import_react.createContext)(void 0);
function WagmiProvider(parameters) {
	const { children, config } = parameters;
	const props = { value: config };
	return (0, import_react.createElement)(Hydrate, parameters, (0, import_react.createElement)(WagmiContext.Provider, props, children));
}
//#endregion
//#region node_modules/wagmi/dist/esm/version.js
var version = "3.6.16";
//#endregion
//#region node_modules/wagmi/dist/esm/utils/getVersion.js
var getVersion = () => `wagmi@${version}`;
//#endregion
//#region node_modules/wagmi/dist/esm/errors/base.js
var BaseError = class extends BaseError$1 {
	constructor() {
		super(...arguments);
		Object.defineProperty(this, "name", {
			enumerable: true,
			configurable: true,
			writable: true,
			value: "WagmiError"
		});
	}
	get docsBaseUrl() {
		return "https://wagmi.sh/react";
	}
	get version() {
		return getVersion();
	}
};
//#endregion
//#region node_modules/wagmi/dist/esm/errors/context.js
var WagmiProviderNotFoundError = class extends BaseError {
	constructor() {
		super("`useConfig` must be used within `WagmiProvider`.", { docsPath: "/api/WagmiProvider" });
		Object.defineProperty(this, "name", {
			enumerable: true,
			configurable: true,
			writable: true,
			value: "WagmiProviderNotFoundError"
		});
	}
};
//#endregion
//#region node_modules/@wagmi/core/dist/esm/query/utils.js
function structuralSharing(oldData, newData) {
	return replaceEqualDeep(oldData, newData);
}
function hashFn(queryKey) {
	return JSON.stringify(queryKey, (_, value) => {
		if (isPlainObject$1(value)) return Object.keys(value).sort().reduce((result, key) => {
			result[key] = value[key];
			return result;
		}, {});
		if (typeof value === "bigint") return value.toString();
		return value;
	});
}
function isPlainObject$1(value) {
	if (!hasObjectPrototype(value)) return false;
	const ctor = value.constructor;
	if (typeof ctor === "undefined") return true;
	const prot = ctor.prototype;
	if (!hasObjectPrototype(prot)) return false;
	if (!prot.hasOwnProperty("isPrototypeOf")) return false;
	return true;
}
function hasObjectPrototype(o) {
	return Object.prototype.toString.call(o) === "[object Object]";
}
function filterQueryOptions(options) {
	const { _defaulted, behavior, gcTime, initialData, initialDataUpdatedAt, maxPages, meta, networkMode, queryFn, queryHash, queryKey, queryKeyHashFn, retry, retryDelay, structuralSharing, getPreviousPageParam, getNextPageParam, initialPageParam, _optimisticResults, enabled, notifyOnChangeProps, placeholderData, refetchInterval, refetchIntervalInBackground, refetchOnMount, refetchOnReconnect, refetchOnWindowFocus, retryOnMount, select, staleTime, suspense, throwOnError, abi, config, connector, query, watch, ...rest } = options;
	if (connector) return {
		connectorUid: connector?.uid,
		...rest
	};
	return rest;
}
//#endregion
//#region node_modules/@wagmi/core/dist/esm/query/call.js
function callQueryOptions(config, options = {}) {
	return {
		...options.query,
		queryFn: async (context) => {
			const [, { scopeKey: _, ...parameters }] = context.queryKey;
			return await call(config, { ...parameters }) ?? null;
		},
		queryKey: callQueryKey(options)
	};
}
function callQueryKey(options = {}) {
	return ["call", filterQueryOptions(options)];
}
//#endregion
//#region node_modules/@wagmi/core/dist/esm/query/connect.js
function connectMutationOptions(config, options = {}) {
	return {
		...options.mutation,
		mutationFn: async (variables) => {
			return connect(config, variables);
		},
		mutationKey: ["connect"]
	};
}
//#endregion
//#region node_modules/@wagmi/core/dist/esm/query/deployContract.js
function deployContractMutationOptions(config, options = {}) {
	return {
		...options.mutation,
		mutationFn(variables) {
			return deployContract(config, variables);
		},
		mutationKey: ["deployContract"]
	};
}
//#endregion
//#region node_modules/@wagmi/core/dist/esm/query/disconnect.js
function disconnectMutationOptions(config, options = {}) {
	return {
		...options.mutation,
		mutationFn: async (variables) => {
			return disconnect(config, variables);
		},
		mutationKey: ["disconnect"]
	};
}
//#endregion
//#region node_modules/@wagmi/core/dist/esm/query/estimateFeesPerGas.js
function estimateFeesPerGasQueryOptions(config, options = {}) {
	return {
		...options.query,
		queryFn: async (context) => {
			const [, { scopeKey: _, ...parameters }] = context.queryKey;
			return estimateFeesPerGas(config, parameters);
		},
		queryKey: estimateFeesPerGasQueryKey(options)
	};
}
function estimateFeesPerGasQueryKey(options = {}) {
	return ["estimateFeesPerGas", filterQueryOptions(options)];
}
//#endregion
//#region node_modules/@wagmi/core/dist/esm/query/estimateGas.js
function estimateGasQueryOptions(config, options = {}) {
	return {
		...options.query,
		enabled: Boolean((options.account || options.connector) && (options.query?.enabled ?? true)),
		queryFn: async (context) => {
			const [, { scopeKey: _, ...parameters }] = context.queryKey;
			if (!parameters.account && !options.connector) throw new Error("account or connector is required");
			return estimateGas(config, {
				...parameters,
				account: parameters.account,
				connector: options.connector
			});
		},
		queryKey: estimateGasQueryKey(options)
	};
}
function estimateGasQueryKey(options = {}) {
	return ["estimateGas", filterQueryOptions(options)];
}
//#endregion
//#region node_modules/@wagmi/core/dist/esm/query/estimateMaxPriorityFeePerGas.js
function estimateMaxPriorityFeePerGasQueryOptions(config, options = {}) {
	return {
		...options.query,
		queryFn: async (context) => {
			const [, { scopeKey: _, ...parameters }] = context.queryKey;
			return estimateMaxPriorityFeePerGas(config, parameters);
		},
		queryKey: estimateMaxPriorityFeePerGasQueryKey(options)
	};
}
function estimateMaxPriorityFeePerGasQueryKey(options = {}) {
	return ["estimateMaxPriorityFeePerGas", filterQueryOptions(options)];
}
//#endregion
//#region node_modules/@wagmi/core/dist/esm/query/getBalance.js
function getBalanceQueryOptions(config, options = {}) {
	return {
		...options.query,
		enabled: Boolean(options.address && (options.query?.enabled ?? true)),
		queryFn: async (context) => {
			const [, { scopeKey: _, ...parameters }] = context.queryKey;
			if (!parameters.address) throw new Error("address is required");
			return await getBalance(config, {
				...parameters,
				address: parameters.address
			}) ?? null;
		},
		queryKey: getBalanceQueryKey(options)
	};
}
function getBalanceQueryKey(options = {}) {
	return ["balance", filterQueryOptions(options)];
}
//#endregion
//#region node_modules/@wagmi/core/dist/esm/query/getBlobBaseFee.js
function getBlobBaseFeeQueryOptions(config, options = {}) {
	return {
		...options.query,
		queryFn: async (context) => {
			const [, { scopeKey: _, ...parameters }] = context.queryKey;
			return await getBlobBaseFee(config, parameters) ?? null;
		},
		queryKey: getBlobBaseFeeQueryKey(options)
	};
}
function getBlobBaseFeeQueryKey(options = {}) {
	return ["blobBaseFee", filterQueryOptions(options)];
}
//#endregion
//#region node_modules/@wagmi/core/dist/esm/query/getBlock.js
function getBlockQueryOptions(config, options = {}) {
	return {
		...options.query,
		queryFn: async (context) => {
			const [, { scopeKey: _, ...parameters }] = context.queryKey;
			return await getBlock(config, parameters) ?? null;
		},
		queryKey: getBlockQueryKey(options)
	};
}
function getBlockQueryKey(options = {}) {
	return ["block", filterQueryOptions(options)];
}
//#endregion
//#region node_modules/@wagmi/core/dist/esm/query/getBlockNumber.js
function getBlockNumberQueryOptions(config, options = {}) {
	return {
		...options.query,
		gcTime: 0,
		queryFn: async (context) => {
			const [, { scopeKey: _, ...parameters }] = context.queryKey;
			return await getBlockNumber(config, parameters) ?? null;
		},
		queryKey: getBlockNumberQueryKey(options)
	};
}
function getBlockNumberQueryKey(options = {}) {
	return ["blockNumber", filterQueryOptions(options)];
}
//#endregion
//#region node_modules/@wagmi/core/dist/esm/query/getBlockTransactionCount.js
function getBlockTransactionCountQueryOptions(config, options = {}) {
	return {
		...options.query,
		queryFn: async (context) => {
			const [, { scopeKey: _, ...parameters }] = context.queryKey;
			return await getBlockTransactionCount(config, parameters) ?? null;
		},
		queryKey: getBlockTransactionCountQueryKey(options)
	};
}
function getBlockTransactionCountQueryKey(options = {}) {
	return ["blockTransactionCount", filterQueryOptions(options)];
}
//#endregion
//#region node_modules/@wagmi/core/dist/esm/query/getBytecode.js
function getBytecodeQueryOptions(config, options = {}) {
	return {
		...options.query,
		enabled: Boolean(options.address && (options.query?.enabled ?? true)),
		queryFn: async (context) => {
			const [, { scopeKey: _, ...parameters }] = context.queryKey;
			if (!parameters.address) throw new Error("address is required");
			return await getBytecode(config, {
				...parameters,
				address: parameters.address
			}) ?? null;
		},
		queryKey: getBytecodeQueryKey(options)
	};
}
function getBytecodeQueryKey(options = {}) {
	return ["getBytecode", filterQueryOptions(options)];
}
//#endregion
//#region node_modules/@wagmi/core/dist/esm/query/getCallsStatus.js
function getCallsStatusQueryOptions(config, options) {
	return {
		...options.query,
		enabled: Boolean(options.connector?.getProvider && (options.query?.enabled ?? true)),
		queryFn: async (context) => {
			if (!options.connector?.getProvider) throw new Error("connector is required");
			const [, { connectorUid: _, scopeKey: __, ...parameters }] = context.queryKey;
			return await getCallsStatus(config, parameters);
		},
		queryKey: getCallsStatusQueryKey(options)
	};
}
function getCallsStatusQueryKey(options) {
	return ["callsStatus", filterQueryOptions(options)];
}
//#endregion
//#region node_modules/@wagmi/core/dist/esm/query/getCapabilities.js
function getCapabilitiesQueryOptions(config, options = {}) {
	return {
		...options.query,
		enabled: Boolean(options.connector?.getProvider && (options.query?.enabled ?? true)),
		queryFn: async (context) => {
			if (!options.connector?.getProvider) throw new Error("connector is required");
			const [, { connectorUid: _, scopeKey: __, ...parameters }] = context.queryKey;
			return await getCapabilities(config, parameters);
		},
		queryKey: getCapabilitiesQueryKey(options)
	};
}
function getCapabilitiesQueryKey(options = {}) {
	return ["capabilities", filterQueryOptions(options)];
}
//#endregion
//#region node_modules/@wagmi/core/dist/esm/query/getConnectorClient.js
function getConnectorClientQueryOptions(config, options = {}) {
	return {
		...options.query,
		enabled: Boolean(options.connector?.getProvider && (options.query?.enabled ?? true)),
		gcTime: 0,
		queryFn: async (context) => {
			const [, { connectorUid: _, scopeKey: __, ...parameters }] = context.queryKey;
			return getConnectorClient(config, {
				...parameters,
				connector: options.connector
			});
		},
		queryKey: getConnectorClientQueryKey(options),
		staleTime: Number.POSITIVE_INFINITY
	};
}
function getConnectorClientQueryKey(options = {}) {
	return ["connectorClient", filterQueryOptions(options)];
}
//#endregion
//#region node_modules/@wagmi/core/dist/esm/query/getContractEvents.js
function getContractEventsQueryOptions(config, options = {}) {
	return {
		...options.query,
		enabled: Boolean(options.abi && (options.query?.enabled ?? true)),
		queryFn: async (context) => {
			if (!options.abi) throw new Error("abi is required");
			const [, { scopeKey: _, ...parameters }] = context.queryKey;
			return await getContractEvents(config, {
				...parameters,
				abi: options.abi
			});
		},
		queryKey: getContractEventsQueryKey(options),
		structuralSharing
	};
}
function getContractEventsQueryKey(options = {}) {
	return ["getContractEvents", filterQueryOptions(options)];
}
//#endregion
//#region node_modules/@wagmi/core/dist/esm/query/getEnsAddress.js
function getEnsAddressQueryOptions(config, options = {}) {
	return {
		...options.query,
		enabled: Boolean(options.name && (options.query?.enabled ?? true)),
		queryFn: async (context) => {
			const [, { scopeKey: _, ...parameters }] = context.queryKey;
			if (!parameters.name) throw new Error("name is required");
			return getEnsAddress(config, {
				...parameters,
				name: parameters.name
			});
		},
		queryKey: getEnsAddressQueryKey(options)
	};
}
function getEnsAddressQueryKey(options = {}) {
	return ["ensAddress", filterQueryOptions(options)];
}
//#endregion
//#region node_modules/@wagmi/core/dist/esm/query/getEnsAvatar.js
function getEnsAvatarQueryOptions(config, options = {}) {
	return {
		...options.query,
		enabled: Boolean(options.name && (options.query?.enabled ?? true)),
		queryFn: async (context) => {
			const [, { scopeKey: _, ...parameters }] = context.queryKey;
			if (!parameters.name) throw new Error("name is required");
			return getEnsAvatar(config, {
				...parameters,
				name: parameters.name
			});
		},
		queryKey: getEnsAvatarQueryKey(options)
	};
}
function getEnsAvatarQueryKey(options = {}) {
	return ["ensAvatar", filterQueryOptions(options)];
}
//#endregion
//#region node_modules/@wagmi/core/dist/esm/query/getEnsName.js
function getEnsNameQueryOptions(config, options = {}) {
	return {
		...options.query,
		enabled: Boolean(options.address && (options.query?.enabled ?? true)),
		queryFn: async (context) => {
			const [, { scopeKey: _, ...parameters }] = context.queryKey;
			if (!parameters.address) throw new Error("address is required");
			return getEnsName(config, {
				...parameters,
				address: parameters.address
			});
		},
		queryKey: getEnsNameQueryKey(options)
	};
}
function getEnsNameQueryKey(options = {}) {
	return ["ensName", filterQueryOptions(options)];
}
//#endregion
//#region node_modules/@wagmi/core/dist/esm/query/getEnsResolver.js
function getEnsResolverQueryOptions(config, options = {}) {
	return {
		...options.query,
		enabled: Boolean(options.name && (options.query?.enabled ?? true)),
		queryFn: async (context) => {
			const [, { scopeKey: _, ...parameters }] = context.queryKey;
			if (!parameters.name) throw new Error("name is required");
			return getEnsResolver(config, {
				...parameters,
				name: parameters.name
			});
		},
		queryKey: getEnsResolverQueryKey(options)
	};
}
function getEnsResolverQueryKey(options = {}) {
	return ["ensResolver", filterQueryOptions(options)];
}
//#endregion
//#region node_modules/@wagmi/core/dist/esm/query/getEnsText.js
function getEnsTextQueryOptions(config, options = {}) {
	return {
		...options.query,
		enabled: Boolean(options.key && options.name && (options.query?.enabled ?? true)),
		queryFn: async (context) => {
			const [, { scopeKey: _, ...parameters }] = context.queryKey;
			if (!parameters.key || !parameters.name) throw new Error("key and name are required");
			return getEnsText(config, {
				...parameters,
				key: parameters.key,
				name: parameters.name
			});
		},
		queryKey: getEnsTextQueryKey(options)
	};
}
function getEnsTextQueryKey(options = {}) {
	return ["ensText", filterQueryOptions(options)];
}
//#endregion
//#region node_modules/@wagmi/core/dist/esm/query/getFeeHistory.js
function getFeeHistoryQueryOptions(config, options = {}) {
	return {
		...options.query,
		enabled: Boolean(options.blockCount && options.rewardPercentiles && (options.query?.enabled ?? true)),
		queryFn: async (context) => {
			const [, { scopeKey: _, ...parameters }] = context.queryKey;
			if (!parameters.blockCount) throw new Error("blockCount is required");
			if (!parameters.rewardPercentiles) throw new Error("rewardPercentiles is required");
			return await getFeeHistory(config, {
				...parameters,
				blockCount: parameters.blockCount,
				rewardPercentiles: parameters.rewardPercentiles
			}) ?? null;
		},
		queryKey: getFeeHistoryQueryKey(options)
	};
}
function getFeeHistoryQueryKey(options = {}) {
	return ["feeHistory", filterQueryOptions(options)];
}
//#endregion
//#region node_modules/@wagmi/core/dist/esm/query/getGasPrice.js
function getGasPriceQueryOptions(config, options = {}) {
	return {
		...options.query,
		queryFn: async (context) => {
			const [, { scopeKey: _, ...parameters }] = context.queryKey;
			return await getGasPrice(config, parameters) ?? null;
		},
		queryKey: getGasPriceQueryKey(options)
	};
}
function getGasPriceQueryKey(options = {}) {
	return ["gasPrice", filterQueryOptions(options)];
}
//#endregion
//#region node_modules/@wagmi/core/dist/esm/query/getProof.js
function getProofQueryOptions(config, options = {}) {
	return {
		...options.query,
		enabled: Boolean(options.address && options.storageKeys && (options.query?.enabled ?? true)),
		queryFn: async (context) => {
			const [, { scopeKey: _, ...parameters }] = context.queryKey;
			if (!parameters.address || !parameters.storageKeys) throw new Error("address and storageKeys are required");
			return getProof(config, {
				...parameters,
				address: parameters.address,
				storageKeys: parameters.storageKeys
			});
		},
		queryKey: getProofQueryKey(options)
	};
}
function getProofQueryKey(options = {}) {
	return ["getProof", filterQueryOptions(options)];
}
//#endregion
//#region node_modules/@wagmi/core/dist/esm/query/getStorageAt.js
function getStorageAtQueryOptions(config, options = {}) {
	return {
		...options.query,
		enabled: Boolean(options.address && options.slot && (options.query?.enabled ?? true)),
		queryFn: async (context) => {
			const [, { scopeKey: _, ...parameters }] = context.queryKey;
			if (!parameters.address || !parameters.slot) throw new Error("address and slot are required");
			return getStorageAt(config, {
				...parameters,
				address: parameters.address,
				slot: parameters.slot
			});
		},
		queryKey: getStorageAtQueryKey(options)
	};
}
function getStorageAtQueryKey(options = {}) {
	return ["getStorageAt", filterQueryOptions(options)];
}
//#endregion
//#region node_modules/@wagmi/core/dist/esm/query/getTransaction.js
function getTransactionQueryOptions(config, options = {}) {
	return {
		...options.query,
		enabled: Boolean((options.hash || options.index && (options.blockHash || options.blockNumber || options.blockTag)) && (options.query?.enabled ?? true)),
		queryFn: async (context) => {
			const [, { scopeKey: _, ...parameters }] = context.queryKey;
			if (!(parameters.hash || parameters.index && (parameters.blockHash || parameters.blockNumber || parameters.blockTag))) throw new Error("hash OR index AND blockHash, blockNumber, blockTag is required");
			return getTransaction(config, parameters);
		},
		queryKey: getTransactionQueryKey(options)
	};
}
function getTransactionQueryKey(options = {}) {
	return ["transaction", filterQueryOptions(options)];
}
//#endregion
//#region node_modules/@wagmi/core/dist/esm/query/getTransactionConfirmations.js
function getTransactionConfirmationsQueryOptions(config, options = {}) {
	return {
		...options.query,
		enabled: Boolean((options.hash || options.transactionReceipt) && (options.query?.enabled ?? true)),
		queryFn: async (context) => {
			const [, { scopeKey: _, ...parameters }] = context.queryKey;
			if (!parameters.hash && !parameters.transactionReceipt) throw new Error("hash or transactionReceipt is required");
			return await getTransactionConfirmations(config, {
				...parameters,
				hash: parameters.hash,
				transactionReceipt: parameters.transactionReceipt
			}) ?? null;
		},
		queryKey: getTransactionConfirmationsQueryKey(options)
	};
}
function getTransactionConfirmationsQueryKey(options = {}) {
	return ["transactionConfirmations", filterQueryOptions(options)];
}
//#endregion
//#region node_modules/@wagmi/core/dist/esm/query/getTransactionCount.js
function getTransactionCountQueryOptions(config, options = {}) {
	return {
		...options.query,
		enabled: Boolean(options.address && (options.query?.enabled ?? true)),
		queryFn: async (context) => {
			const [, { scopeKey: _, ...parameters }] = context.queryKey;
			if (!parameters.address) throw new Error("address is required");
			return await getTransactionCount(config, {
				...parameters,
				address: parameters.address
			}) ?? null;
		},
		queryKey: getTransactionCountQueryKey(options)
	};
}
function getTransactionCountQueryKey(options = {}) {
	return ["transactionCount", filterQueryOptions(options)];
}
//#endregion
//#region node_modules/@wagmi/core/dist/esm/query/getTransactionReceipt.js
function getTransactionReceiptQueryOptions(config, options = {}) {
	return {
		...options.query,
		enabled: Boolean(options.hash && (options.query?.enabled ?? true)),
		queryFn: async (context) => {
			const [, { scopeKey: _, ...parameters }] = context.queryKey;
			if (!parameters.hash) throw new Error("hash is required");
			return getTransactionReceipt(config, {
				...parameters,
				hash: parameters.hash
			});
		},
		queryKey: getTransactionReceiptQueryKey(options)
	};
}
function getTransactionReceiptQueryKey(options = {}) {
	return ["getTransactionReceipt", filterQueryOptions(options)];
}
//#endregion
//#region node_modules/@wagmi/core/dist/esm/query/getWalletClient.js
function getWalletClientQueryOptions(config, options = {}) {
	return {
		...options.query,
		enabled: Boolean(options.connector?.getProvider && (options.query?.enabled ?? true)),
		gcTime: 0,
		queryFn: async (context) => {
			if (!options.connector?.getProvider) throw new Error("connector is required");
			const [, { connectorUid: _, scopeKey: __, ...parameters }] = context.queryKey;
			return getWalletClient(config, {
				...parameters,
				connector: options.connector
			});
		},
		queryKey: getWalletClientQueryKey(options),
		staleTime: Number.POSITIVE_INFINITY
	};
}
function getWalletClientQueryKey(options = {}) {
	return ["walletClient", filterQueryOptions(options)];
}
//#endregion
//#region node_modules/@wagmi/core/dist/esm/query/infiniteReadContracts.js
function infiniteReadContractsQueryOptions(config, options) {
	return {
		...options.query,
		async queryFn({ pageParam, queryKey }) {
			const { contracts } = options;
			const { cacheKey: _, scopeKey: _s, ...parameters } = queryKey[1];
			return await readContracts(config, {
				...parameters,
				contracts: contracts(pageParam)
			});
		},
		queryKey: infiniteReadContractsQueryKey(options)
	};
}
function infiniteReadContractsQueryKey(options) {
	const { contracts: _, ...parameters } = options;
	return ["infiniteReadContracts", filterQueryOptions(parameters)];
}
//#endregion
//#region node_modules/@wagmi/core/dist/esm/query/prepareTransactionRequest.js
function prepareTransactionRequestQueryOptions(config, options = {}) {
	const hasCalls = Array.isArray(options.calls) && options.calls.length > 0;
	return {
		...options.query,
		enabled: Boolean((options.to || hasCalls) && (options.query?.enabled ?? true)),
		queryFn: async (context) => {
			const [, { scopeKey: _, ...parameters }] = context.queryKey;
			const hasCalls = Array.isArray(parameters.calls) && parameters.calls.length > 0;
			if (!parameters.to && !hasCalls) throw new Error("to or calls is required");
			return prepareTransactionRequest(config, { ...parameters });
		},
		queryKey: prepareTransactionRequestQueryKey(options)
	};
}
function prepareTransactionRequestQueryKey(options = {}) {
	return ["prepareTransactionRequest", filterQueryOptions(options)];
}
//#endregion
//#region node_modules/@wagmi/core/dist/esm/query/readContract.js
function readContractQueryOptions(config, options = {}) {
	return {
		...options.query,
		enabled: Boolean(Boolean(options.address || "code" in options && options.code) && options.abi && options.functionName && (options.query?.enabled ?? true)),
		queryFn: async (context) => {
			if (!options.abi) throw new Error("abi is required");
			const [, { scopeKey: _, ...parameters }] = context.queryKey;
			if (!parameters.functionName) throw new Error("functionName is required");
			return await readContract(config, {
				...parameters,
				abi: options.abi,
				address: parameters.address,
				code: "code" in parameters && parameters.code ? parameters.code : void 0,
				functionName: parameters.functionName
			});
		},
		queryKey: readContractQueryKey(options),
		structuralSharing
	};
}
function readContractQueryKey(options = {}) {
	return ["readContract", filterQueryOptions(options)];
}
//#endregion
//#region node_modules/@wagmi/core/dist/esm/query/readContracts.js
function readContractsQueryOptions(config, options = {}) {
	return {
		...options.query,
		queryFn: async (context) => {
			const contracts = [];
			const length = context.queryKey[1].contracts.length;
			for (let i = 0; i < length; i++) {
				const contract = context.queryKey[1].contracts[i];
				const abi = (options.contracts?.[i]).abi;
				contracts.push({
					...contract,
					abi
				});
			}
			const { scopeKey: _, ...parameters } = context.queryKey[1];
			return readContracts(config, {
				...parameters,
				contracts
			});
		},
		queryKey: readContractsQueryKey(options)
	};
}
function readContractsQueryKey(options = {}) {
	const contracts = [];
	let hasContractWithoutChainId = false;
	for (const contract of options.contracts ?? []) {
		const { abi: _, ...rest } = contract;
		if (rest.chainId === void 0) hasContractWithoutChainId = true;
		const chainId = rest.chainId ?? options.chainId;
		contracts.push({
			...rest,
			...chainId ? { chainId } : {}
		});
	}
	const { chainId: _, ...rest } = options;
	return ["readContracts", filterQueryOptions({
		...rest,
		...hasContractWithoutChainId && options.chainId ? { chainId: options.chainId } : {},
		contracts
	})];
}
//#endregion
//#region node_modules/@wagmi/core/dist/esm/query/reconnect.js
function reconnectMutationOptions(config, options = {}) {
	return {
		...options.mutation,
		mutationFn(variables) {
			return reconnect(config, variables);
		},
		mutationKey: ["reconnect"]
	};
}
//#endregion
//#region node_modules/@wagmi/core/dist/esm/query/sendCalls.js
function sendCallsMutationOptions(config, options = {}) {
	return {
		...options.mutation,
		mutationFn(variables) {
			return sendCalls(config, variables);
		},
		mutationKey: ["sendCalls"]
	};
}
//#endregion
//#region node_modules/@wagmi/core/dist/esm/query/sendCallsSync.js
function sendCallsSyncMutationOptions(config, options = {}) {
	return {
		...options.mutation,
		mutationFn(variables) {
			return sendCallsSync(config, variables);
		},
		mutationKey: ["sendCallsSync"]
	};
}
//#endregion
//#region node_modules/@wagmi/core/dist/esm/query/sendTransaction.js
function sendTransactionMutationOptions(config, options = {}) {
	return {
		...options.mutation,
		mutationFn(variables) {
			return sendTransaction(config, variables);
		},
		mutationKey: ["sendTransaction"]
	};
}
//#endregion
//#region node_modules/@wagmi/core/dist/esm/query/sendTransactionSync.js
function sendTransactionSyncMutationOptions(config, options = {}) {
	return {
		...options.mutation,
		mutationFn(variables) {
			return sendTransactionSync(config, variables);
		},
		mutationKey: ["sendTransactionSync"]
	};
}
//#endregion
//#region node_modules/@wagmi/core/dist/esm/query/showCallsStatus.js
function showCallsStatusMutationOptions(config, options = {}) {
	return {
		...options.mutation,
		mutationFn(variables) {
			return showCallsStatus(config, variables);
		},
		mutationKey: ["showCallsStatus"]
	};
}
//#endregion
//#region node_modules/@wagmi/core/dist/esm/query/signMessage.js
function signMessageMutationOptions(config, options = {}) {
	return {
		...options.mutation,
		mutationFn(variables) {
			return signMessage(config, variables);
		},
		mutationKey: ["signMessage"]
	};
}
//#endregion
//#region node_modules/@wagmi/core/dist/esm/query/signTransaction.js
function signTransactionMutationOptions(config, options = {}) {
	return {
		...options.mutation,
		mutationFn(variables) {
			return signTransaction(config, variables);
		},
		mutationKey: ["signTransaction"]
	};
}
//#endregion
//#region node_modules/@wagmi/core/dist/esm/query/signTypedData.js
function signTypedDataMutationOptions(config, options = {}) {
	return {
		...options.mutation,
		mutationFn(variables) {
			return signTypedData(config, variables);
		},
		mutationKey: ["signTypedData"]
	};
}
//#endregion
//#region node_modules/@wagmi/core/dist/esm/query/simulateContract.js
function simulateContractQueryOptions(config, options = {}) {
	return {
		...options.query,
		enabled: Boolean(options.abi && options.address && options.connector && options.functionName && (options.query?.enabled ?? true)),
		queryFn: async (context) => {
			if (!options.abi) throw new Error("abi is required");
			if (!options.connector) throw new Error("connector is required");
			const [, { scopeKey: _, ...parameters }] = context.queryKey;
			if (!parameters.address) throw new Error("address is required");
			if (!parameters.functionName) throw new Error("functionName is required");
			return simulateContract(config, {
				...parameters,
				abi: options.abi,
				address: parameters.address,
				connector: options.connector,
				functionName: parameters.functionName
			});
		},
		queryKey: simulateContractQueryKey(options)
	};
}
function simulateContractQueryKey(options = {}) {
	const { connector: _, ...rest } = options;
	return ["simulateContract", filterQueryOptions(rest)];
}
//#endregion
//#region node_modules/@wagmi/core/dist/esm/query/switchChain.js
function switchChainMutationOptions(config, options = {}) {
	return {
		...options.mutation,
		mutationFn(variables) {
			return switchChain(config, variables);
		},
		mutationKey: ["switchChain"]
	};
}
//#endregion
//#region node_modules/@wagmi/core/dist/esm/query/switchConnection.js
function switchConnectionMutationOptions(config, options = {}) {
	return {
		...options.mutation,
		mutationFn(variables) {
			return switchConnection(config, variables);
		},
		mutationKey: ["switchConnection"]
	};
}
//#endregion
//#region node_modules/@wagmi/core/dist/esm/query/verifyMessage.js
function verifyMessageQueryOptions(config, options = {}) {
	return {
		...options.query,
		enabled: Boolean(options.address && options.message && options.signature && (options.query?.enabled ?? true)),
		queryFn: async (context) => {
			const [, { scopeKey: _, ...parameters }] = context.queryKey;
			if (!parameters.address) throw new Error("address is required");
			if (!parameters.message) throw new Error("message is required");
			if (!parameters.signature) throw new Error("signature is required");
			return await verifyMessage(config, {
				...parameters,
				address: parameters.address,
				message: parameters.message,
				signature: parameters.signature
			}) ?? null;
		},
		queryKey: verifyMessageQueryKey(options)
	};
}
function verifyMessageQueryKey(options = {}) {
	return ["verifyMessage", filterQueryOptions(options)];
}
//#endregion
//#region node_modules/@wagmi/core/dist/esm/query/verifyTypedData.js
function verifyTypedDataQueryOptions(config, options = {}) {
	return {
		...options.query,
		enabled: Boolean(options.address && options.message && options.primaryType && options.signature && options.types && (options.query?.enabled ?? true)),
		queryFn: async (context) => {
			const [, { scopeKey: _, ...parameters }] = context.queryKey;
			if (!parameters.address) throw new Error("address is required");
			if (!parameters.message) throw new Error("message is required");
			if (!parameters.primaryType) throw new Error("primaryType is required");
			if (!parameters.signature) throw new Error("signature is required");
			if (!parameters.types) throw new Error("types is required");
			return await verifyTypedData(config, {
				...parameters,
				address: parameters.address,
				message: parameters.message,
				primaryType: parameters.primaryType,
				signature: parameters.signature,
				types: parameters.types
			}) ?? null;
		},
		queryKey: verifyTypedDataQueryKey(options)
	};
}
function verifyTypedDataQueryKey(options = {}) {
	return ["verifyTypedData", filterQueryOptions(options)];
}
//#endregion
//#region node_modules/@wagmi/core/dist/esm/query/waitForCallsStatus.js
function waitForCallsStatusQueryOptions(config, options) {
	return {
		...options.query,
		enabled: Boolean(options.id && options.connector?.getProvider && (options.query?.enabled ?? true)),
		queryFn: async (context) => {
			if (!options.connector?.getProvider) throw new Error("connector is required");
			const [, { connectorUid: _, scopeKey: __, ...parameters }] = context.queryKey;
			if (!parameters.id) throw new Error("id is required");
			return await waitForCallsStatus(config, {
				...parameters,
				id: parameters.id
			});
		},
		queryKey: waitForCallsStatusQueryKey(options)
	};
}
function waitForCallsStatusQueryKey(options = {}) {
	return ["callsStatus", filterQueryOptions(options)];
}
//#endregion
//#region node_modules/@wagmi/core/dist/esm/query/waitForTransactionReceipt.js
function waitForTransactionReceiptQueryOptions(config, options = {}) {
	return {
		...options.query,
		enabled: Boolean(options.hash && (options.query?.enabled ?? true)),
		queryFn: async (context) => {
			const [, { scopeKey: _, ...parameters }] = context.queryKey;
			if (!parameters.hash) throw new Error("hash is required");
			return waitForTransactionReceipt(config, {
				...parameters,
				onReplaced: options.onReplaced,
				hash: parameters.hash
			});
		},
		queryKey: waitForTransactionReceiptQueryKey(options)
	};
}
function waitForTransactionReceiptQueryKey(options = {}) {
	const { onReplaced: _, ...rest } = options;
	return ["waitForTransactionReceipt", filterQueryOptions(rest)];
}
//#endregion
//#region node_modules/@wagmi/core/dist/esm/query/watchAsset.js
function watchAssetMutationOptions(config, options = {}) {
	return {
		...options.mutation,
		mutationFn(variables) {
			return watchAsset(config, variables);
		},
		mutationKey: ["watchAsset"]
	};
}
//#endregion
//#region node_modules/@wagmi/core/dist/esm/query/writeContract.js
function writeContractMutationOptions(config, options = {}) {
	return {
		...options.mutation,
		mutationFn(variables) {
			return writeContract(config, variables);
		},
		mutationKey: ["writeContract"]
	};
}
//#endregion
//#region node_modules/@wagmi/core/dist/esm/query/writeContractSync.js
function writeContractSyncMutationOptions(config, options = {}) {
	return {
		...options.mutation,
		mutationFn(variables) {
			return writeContractSync(config, variables);
		},
		mutationKey: ["writeContractSync"]
	};
}
//#endregion
//#region node_modules/wagmi/dist/esm/utils/query.js
function useQuery(parameters) {
	const result = useQuery$1({
		...parameters,
		queryKeyHashFn: hashFn
	});
	result.queryKey = parameters.queryKey;
	return result;
}
function useInfiniteQuery(parameters) {
	const result = useInfiniteQuery$1({
		...parameters,
		queryKeyHashFn: hashFn
	});
	result.queryKey = parameters.queryKey;
	return result;
}
//#endregion
//#region node_modules/wagmi/dist/esm/hooks/useConfig.js
/** https://wagmi.sh/react/api/hooks/useConfig */
function useConfig(parameters = {}) {
	const config = parameters.config ?? (0, import_react.useContext)(WagmiContext);
	if (!config) throw new WagmiProviderNotFoundError();
	return config;
}
//#endregion
//#region node_modules/wagmi/dist/esm/hooks/useChainId.js
/** https://wagmi.sh/react/api/hooks/useChainId */
function useChainId(parameters = {}) {
	const config = useConfig(parameters);
	return (0, import_react.useSyncExternalStore)((onChange) => watchChainId(config, { onChange }), () => getChainId(config), () => getChainId(config));
}
//#endregion
//#region node_modules/wagmi/dist/esm/hooks/useBalance.js
/** https://wagmi.sh/react/api/hooks/useBalance */
function useBalance(parameters = {}) {
	const config = useConfig(parameters);
	const chainId = useChainId({ config });
	return useQuery(getBalanceQueryOptions(config, {
		...parameters,
		chainId: parameters.chainId ?? chainId
	}));
}
//#endregion
//#region node_modules/wagmi/dist/esm/hooks/useBlobBaseFee.js
/** https://wagmi.sh/react/api/hooks/useBlobBaseFee */
function useBlobBaseFee(parameters = {}) {
	const config = useConfig(parameters);
	const chainId = useChainId({ config });
	return useQuery(getBlobBaseFeeQueryOptions(config, {
		...parameters,
		chainId: parameters.chainId ?? chainId
	}));
}
//#endregion
//#region node_modules/wagmi/dist/esm/hooks/useWatchBlocks.js
/** https://wagmi.sh/react/hooks/useWatchBlocks */
function useWatchBlocks(parameters = {}) {
	const { enabled = true, onBlock, config: _, ...rest } = parameters;
	const config = useConfig(parameters);
	const configChainId = useChainId({ config });
	const chainId = parameters.chainId ?? configChainId;
	const onBlockRef = (0, import_react.useRef)(onBlock);
	const onErrorRef = (0, import_react.useRef)(rest.onError);
	onBlockRef.current = onBlock;
	onErrorRef.current = rest.onError;
	(0, import_react.useEffect)(() => {
		if (!enabled) return;
		if (!onBlockRef.current) return;
		return watchBlocks(config, {
			...rest,
			chainId,
			onBlock: (block, prevBlock) => onBlockRef.current?.(block, prevBlock),
			onError: (error) => onErrorRef.current?.(error)
		});
	}, [
		chainId,
		config,
		enabled,
		rest.blockTag,
		rest.emitMissed,
		rest.emitOnBegin,
		rest.includeTransactions,
		rest.poll,
		rest.pollingInterval,
		rest.syncConnectedChain
	]);
}
//#endregion
//#region node_modules/wagmi/dist/esm/hooks/useBlock.js
/** https://wagmi.sh/react/hooks/useBlock */
function useBlock(parameters = {}) {
	const config = useConfig(parameters);
	const chainId = useChainId({ config });
	const options = getBlockQueryOptions(config, {
		...parameters,
		chainId: parameters.chainId ?? chainId
	});
	const queryClient = useQueryClient();
	useWatchBlocks({
		config: parameters.config,
		chainId: parameters.chainId,
		...typeof parameters.watch === "object" ? parameters.watch : {},
		enabled: Boolean((options.enabled ?? true) && (typeof parameters.watch === "object" ? parameters.watch.enabled : parameters.watch)),
		onBlock(block) {
			queryClient.setQueryData(options.queryKey, block);
		}
	});
	return useQuery(options);
}
//#endregion
//#region node_modules/wagmi/dist/esm/hooks/useWatchBlockNumber.js
/** https://wagmi.sh/react/api/hooks/useWatchBlockNumber */
function useWatchBlockNumber(parameters = {}) {
	const { enabled = true, onBlockNumber, config: _, ...rest } = parameters;
	const config = useConfig(parameters);
	const configChainId = useChainId({ config });
	const chainId = parameters.chainId ?? configChainId;
	const onBlockNumberRef = (0, import_react.useRef)(onBlockNumber);
	const onErrorRef = (0, import_react.useRef)(rest.onError);
	onBlockNumberRef.current = onBlockNumber;
	onErrorRef.current = rest.onError;
	(0, import_react.useEffect)(() => {
		if (!enabled) return;
		if (!onBlockNumberRef.current) return;
		return watchBlockNumber(config, {
			...rest,
			chainId,
			onBlockNumber: (blockNumber, prevBlockNumber) => onBlockNumberRef.current?.(blockNumber, prevBlockNumber),
			onError: (error) => onErrorRef.current?.(error)
		});
	}, [
		chainId,
		config,
		enabled,
		rest.emitMissed,
		rest.emitOnBegin,
		rest.poll,
		rest.pollingInterval,
		rest.syncConnectedChain
	]);
}
//#endregion
//#region node_modules/wagmi/dist/esm/hooks/useBlockNumber.js
/** https://wagmi.sh/react/api/hooks/useBlockNumber */
function useBlockNumber(parameters = {}) {
	const config = useConfig(parameters);
	const chainId = useChainId({ config });
	const options = getBlockNumberQueryOptions(config, {
		...parameters,
		chainId: parameters.chainId ?? chainId
	});
	const queryClient = useQueryClient();
	useWatchBlockNumber({
		config: parameters.config,
		chainId: parameters.chainId,
		...typeof parameters.watch === "object" ? parameters.watch : {},
		enabled: Boolean((options.enabled ?? true) && (typeof parameters.watch === "object" ? parameters.watch.enabled : parameters.watch)),
		onBlockNumber(blockNumber) {
			queryClient.setQueryData(options.queryKey, blockNumber);
		}
	});
	return useQuery(options);
}
//#endregion
//#region node_modules/wagmi/dist/esm/hooks/useBlockTransactionCount.js
/** https://wagmi.sh/react/api/hooks/useBlockTransactionCount */
function useBlockTransactionCount(parameters = {}) {
	const config = useConfig(parameters);
	const chainId = useChainId({ config });
	return useQuery(getBlockTransactionCountQueryOptions(config, {
		...parameters,
		chainId: parameters.chainId ?? chainId
	}));
}
//#endregion
//#region node_modules/wagmi/dist/esm/hooks/useBytecode.js
/** https://wagmi.sh/react/api/hooks/useBytecode */
function useBytecode(parameters = {}) {
	const config = useConfig(parameters);
	const chainId = useChainId({ config });
	return useQuery(getBytecodeQueryOptions(config, {
		...parameters,
		chainId: parameters.chainId ?? chainId
	}));
}
//#endregion
//#region node_modules/wagmi/dist/esm/hooks/useCall.js
/** https://wagmi.sh/react/api/hooks/useCall */
function useCall(parameters = {}) {
	const config = useConfig(parameters);
	const chainId = useChainId({ config });
	return useQuery(callQueryOptions(config, {
		...parameters,
		chainId: parameters.chainId ?? chainId
	}));
}
//#endregion
//#region node_modules/@wagmi/core/dist/esm/actions/watchChains.js
/**
* @internal
* We don't expose this because as far as consumers know, you can't chainge (lol) `config.chains` at runtime.
* Setting `config.chains` via `config._internal.chains.setState(...)` is an extremely advanced use case that's not worth documenting or supporting in the public API at this time.
*/
function watchChains(config, parameters) {
	const { onChange } = parameters;
	return config._internal.chains.subscribe((chains, prevChains) => {
		onChange(chains, prevChains);
	});
}
//#endregion
//#region node_modules/use-sync-external-store/cjs/use-sync-external-store-shim.development.js
/**
* @license React
* use-sync-external-store-shim.development.js
*
* Copyright (c) Meta Platforms, Inc. and affiliates.
*
* This source code is licensed under the MIT license found in the
* LICENSE file in the root directory of this source tree.
*/
var require_use_sync_external_store_shim_development = /* @__PURE__ */ __commonJSMin(((exports) => {
	(function() {
		function is(x, y) {
			return x === y && (0 !== x || 1 / x === 1 / y) || x !== x && y !== y;
		}
		function useSyncExternalStore$2(subscribe, getSnapshot) {
			didWarnOld18Alpha || void 0 === React.startTransition || (didWarnOld18Alpha = !0, console.error("You are using an outdated, pre-release alpha of React 18 that does not support useSyncExternalStore. The use-sync-external-store shim will not work correctly. Upgrade to a newer pre-release."));
			var value = getSnapshot();
			if (!didWarnUncachedGetSnapshot) {
				var cachedValue = getSnapshot();
				objectIs(value, cachedValue) || (console.error("The result of getSnapshot should be cached to avoid an infinite loop"), didWarnUncachedGetSnapshot = !0);
			}
			cachedValue = useState({ inst: {
				value,
				getSnapshot
			} });
			var inst = cachedValue[0].inst, forceUpdate = cachedValue[1];
			useLayoutEffect(function() {
				inst.value = value;
				inst.getSnapshot = getSnapshot;
				checkIfSnapshotChanged(inst) && forceUpdate({ inst });
			}, [
				subscribe,
				value,
				getSnapshot
			]);
			useEffect(function() {
				checkIfSnapshotChanged(inst) && forceUpdate({ inst });
				return subscribe(function() {
					checkIfSnapshotChanged(inst) && forceUpdate({ inst });
				});
			}, [subscribe]);
			useDebugValue(value);
			return value;
		}
		function checkIfSnapshotChanged(inst) {
			var latestGetSnapshot = inst.getSnapshot;
			inst = inst.value;
			try {
				var nextValue = latestGetSnapshot();
				return !objectIs(inst, nextValue);
			} catch (error) {
				return !0;
			}
		}
		function useSyncExternalStore$1(subscribe, getSnapshot) {
			return getSnapshot();
		}
		"undefined" !== typeof __REACT_DEVTOOLS_GLOBAL_HOOK__ && "function" === typeof __REACT_DEVTOOLS_GLOBAL_HOOK__.registerInternalModuleStart && __REACT_DEVTOOLS_GLOBAL_HOOK__.registerInternalModuleStart(Error());
		var React = require_react(), objectIs = "function" === typeof Object.is ? Object.is : is, useState = React.useState, useEffect = React.useEffect, useLayoutEffect = React.useLayoutEffect, useDebugValue = React.useDebugValue, didWarnOld18Alpha = !1, didWarnUncachedGetSnapshot = !1, shim = "undefined" === typeof window || "undefined" === typeof window.document || "undefined" === typeof window.document.createElement ? useSyncExternalStore$1 : useSyncExternalStore$2;
		exports.useSyncExternalStore = void 0 !== React.useSyncExternalStore ? React.useSyncExternalStore : shim;
		"undefined" !== typeof __REACT_DEVTOOLS_GLOBAL_HOOK__ && "function" === typeof __REACT_DEVTOOLS_GLOBAL_HOOK__.registerInternalModuleStop && __REACT_DEVTOOLS_GLOBAL_HOOK__.registerInternalModuleStop(Error());
	})();
}));
//#endregion
//#region node_modules/use-sync-external-store/shim/index.js
var require_shim = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	module.exports = require_use_sync_external_store_shim_development();
}));
//#endregion
//#region node_modules/use-sync-external-store/cjs/use-sync-external-store-shim/with-selector.development.js
/**
* @license React
* use-sync-external-store-shim/with-selector.development.js
*
* Copyright (c) Meta Platforms, Inc. and affiliates.
*
* This source code is licensed under the MIT license found in the
* LICENSE file in the root directory of this source tree.
*/
var require_with_selector_development = /* @__PURE__ */ __commonJSMin(((exports) => {
	(function() {
		function is(x, y) {
			return x === y && (0 !== x || 1 / x === 1 / y) || x !== x && y !== y;
		}
		"undefined" !== typeof __REACT_DEVTOOLS_GLOBAL_HOOK__ && "function" === typeof __REACT_DEVTOOLS_GLOBAL_HOOK__.registerInternalModuleStart && __REACT_DEVTOOLS_GLOBAL_HOOK__.registerInternalModuleStart(Error());
		var React = require_react(), shim = require_shim(), objectIs = "function" === typeof Object.is ? Object.is : is, useSyncExternalStore = shim.useSyncExternalStore, useRef = React.useRef, useEffect = React.useEffect, useMemo = React.useMemo, useDebugValue = React.useDebugValue;
		exports.useSyncExternalStoreWithSelector = function(subscribe, getSnapshot, getServerSnapshot, selector, isEqual) {
			var instRef = useRef(null);
			if (null === instRef.current) {
				var inst = {
					hasValue: !1,
					value: null
				};
				instRef.current = inst;
			} else inst = instRef.current;
			instRef = useMemo(function() {
				function memoizedSelector(nextSnapshot) {
					if (!hasMemo) {
						hasMemo = !0;
						memoizedSnapshot = nextSnapshot;
						nextSnapshot = selector(nextSnapshot);
						if (void 0 !== isEqual && inst.hasValue) {
							var currentSelection = inst.value;
							if (isEqual(currentSelection, nextSnapshot)) return memoizedSelection = currentSelection;
						}
						return memoizedSelection = nextSnapshot;
					}
					currentSelection = memoizedSelection;
					if (objectIs(memoizedSnapshot, nextSnapshot)) return currentSelection;
					var nextSelection = selector(nextSnapshot);
					if (void 0 !== isEqual && isEqual(currentSelection, nextSelection)) return memoizedSnapshot = nextSnapshot, currentSelection;
					memoizedSnapshot = nextSnapshot;
					return memoizedSelection = nextSelection;
				}
				var hasMemo = !1, memoizedSnapshot, memoizedSelection, maybeGetServerSnapshot = void 0 === getServerSnapshot ? null : getServerSnapshot;
				return [function() {
					return memoizedSelector(getSnapshot());
				}, null === maybeGetServerSnapshot ? void 0 : function() {
					return memoizedSelector(maybeGetServerSnapshot());
				}];
			}, [
				getSnapshot,
				getServerSnapshot,
				selector,
				isEqual
			]);
			var value = useSyncExternalStore(subscribe, instRef[0], instRef[1]);
			useEffect(function() {
				inst.hasValue = !0;
				inst.value = value;
			}, [value]);
			useDebugValue(value);
			return value;
		};
		"undefined" !== typeof __REACT_DEVTOOLS_GLOBAL_HOOK__ && "function" === typeof __REACT_DEVTOOLS_GLOBAL_HOOK__.registerInternalModuleStop && __REACT_DEVTOOLS_GLOBAL_HOOK__.registerInternalModuleStop(Error());
	})();
}));
//#endregion
//#region node_modules/wagmi/dist/esm/hooks/useSyncExternalStoreWithTracked.js
var import_with_selector = (/* @__PURE__ */ __commonJSMin(((exports, module) => {
	module.exports = require_with_selector_development();
})))();
var isPlainObject = (obj) => typeof obj === "object" && !Array.isArray(obj);
function useSyncExternalStoreWithTracked(subscribe, getSnapshot, getServerSnapshot = getSnapshot, isEqual = deepEqual) {
	const trackedKeys = (0, import_react.useRef)([]);
	const result = (0, import_with_selector.useSyncExternalStoreWithSelector)(subscribe, getSnapshot, getServerSnapshot, (x) => x, (a, b) => {
		if (isPlainObject(a) && isPlainObject(b) && trackedKeys.current.length) {
			for (const key of trackedKeys.current) if (!isEqual(a[key], b[key])) return false;
			return true;
		}
		return isEqual(a, b);
	});
	return (0, import_react.useMemo)(() => {
		if (isPlainObject(result)) {
			const trackedResult = { ...result };
			let properties = {};
			for (const [key, value] of Object.entries(trackedResult)) properties = {
				...properties,
				[key]: {
					configurable: false,
					enumerable: true,
					get: () => {
						if (!trackedKeys.current.includes(key)) trackedKeys.current.push(key);
						return value;
					}
				}
			};
			Object.defineProperties(trackedResult, properties);
			return trackedResult;
		}
		return result;
	}, [result]);
}
//#endregion
//#region node_modules/wagmi/dist/esm/hooks/useConnection.js
/** https://wagmi.sh/react/api/hooks/useConnection */
function useConnection(parameters = {}) {
	const config = useConfig(parameters);
	return useSyncExternalStoreWithTracked((onChange) => watchConnection(config, { onChange }), () => getConnection(config));
}
//#endregion
//#region node_modules/wagmi/dist/esm/hooks/useCallsStatus.js
/** https://wagmi.sh/react/api/hooks/useCallsStatus */
function useCallsStatus(parameters) {
	const config = useConfig(parameters);
	const { connector } = useConnection({ config });
	return useQuery(getCallsStatusQueryOptions(config, {
		...parameters,
		connector: parameters.connector ?? connector
	}));
}
//#endregion
//#region node_modules/wagmi/dist/esm/hooks/useCapabilities.js
/** https://wagmi.sh/react/api/hooks/useCapabilities */
function useCapabilities(parameters = {}) {
	const config = useConfig(parameters);
	const { address, connector } = useConnection({ config });
	return useQuery(getCapabilitiesQueryOptions(config, {
		...parameters,
		account: parameters.account ?? address,
		connector: parameters.connector ?? connector
	}));
}
//#endregion
//#region node_modules/wagmi/dist/esm/hooks/useChains.js
/** https://wagmi.sh/react/api/hooks/useChains */
function useChains(parameters = {}) {
	const config = useConfig(parameters);
	return (0, import_react.useSyncExternalStore)((onChange) => watchChains(config, { onChange }), () => getChains(config), () => getChains(config));
}
//#endregion
//#region node_modules/wagmi/dist/esm/hooks/useClient.js
/** https://wagmi.sh/react/api/hooks/useClient */
function useClient(parameters = {}) {
	const config = useConfig(parameters);
	return (0, import_with_selector.useSyncExternalStoreWithSelector)((onChange) => watchClient(config, { onChange }), () => getClient(config, parameters), () => getClient(config, parameters), (x) => x, (a, b) => a?.uid === b?.uid);
}
//#endregion
//#region node_modules/wagmi/dist/esm/hooks/useConnectors.js
/** https://wagmi.sh/react/api/hooks/useConnectors */
function useConnectors(parameters = {}) {
	const config = useConfig(parameters);
	return (0, import_react.useSyncExternalStore)((onChange) => watchConnectors(config, { onChange }), () => getConnectors(config), () => getConnectors(config));
}
//#endregion
//#region node_modules/wagmi/dist/esm/hooks/useConnect.js
/** https://wagmi.sh/react/api/hooks/useConnect */
function useConnect(parameters = {}) {
	const config = useConfig(parameters);
	const mutation = useMutation(connectMutationOptions(config, parameters));
	(0, import_react.useEffect)(() => {
		return config.subscribe(({ status }) => status, (status, previousStatus) => {
			if (previousStatus === "connected" && status === "disconnected") mutation.reset();
		});
	}, [config, mutation.reset]);
	return {
		...mutation,
		connect: mutation.mutate,
		connectAsync: mutation.mutateAsync,
		connectors: useConnectors({ config })
	};
}
//#endregion
//#region node_modules/wagmi/dist/esm/hooks/useConnectionEffect.js
/** https://wagmi.sh/react/api/hooks/useConnectionEffect */
function useConnectionEffect(parameters = {}) {
	const { onConnect, onDisconnect } = parameters;
	const config = useConfig(parameters);
	(0, import_react.useEffect)(() => {
		return watchConnection(config, { onChange(data, prevData) {
			if ((prevData.status === "reconnecting" || prevData.status === "connecting" && prevData.address === void 0) && data.status === "connected") {
				const { address, addresses, chain, chainId, connector } = data;
				const isReconnected = prevData.status === "reconnecting" || prevData.status === void 0;
				onConnect?.({
					address,
					addresses,
					chain,
					chainId,
					connector,
					isReconnected
				});
			} else if (prevData.status === "connected" && data.status === "disconnected") onDisconnect?.();
		} });
	}, [
		config,
		onConnect,
		onDisconnect
	]);
}
//#endregion
//#region node_modules/wagmi/dist/esm/hooks/useConnections.js
/** https://wagmi.sh/react/api/hooks/useConnections */
function useConnections(parameters = {}) {
	const config = useConfig(parameters);
	return (0, import_react.useSyncExternalStore)((onChange) => watchConnections(config, { onChange }), () => getConnections(config), () => getConnections(config));
}
//#endregion
//#region node_modules/wagmi/dist/esm/hooks/useConnectorClient.js
/** https://wagmi.sh/react/api/hooks/useConnectorClient */
function useConnectorClient(parameters = {}) {
	const config = useConfig(parameters);
	const chainId = useChainId({ config });
	const { address, connector } = useConnection({ config });
	const options = getConnectorClientQueryOptions(config, {
		...parameters,
		chainId: parameters.chainId ?? chainId,
		connector: parameters.connector ?? connector,
		query: parameters.query
	});
	const addressRef = (0, import_react.useRef)(address);
	const queryClient = useQueryClient();
	(0, import_react.useEffect)(() => {
		const previousAddress = addressRef.current;
		if (!address && previousAddress) {
			queryClient.removeQueries({ queryKey: options.queryKey });
			addressRef.current = void 0;
		} else if (address !== previousAddress) {
			queryClient.invalidateQueries({ queryKey: options.queryKey });
			addressRef.current = address;
		}
	}, [address, queryClient]);
	return useQuery(options);
}
//#endregion
//#region node_modules/wagmi/dist/esm/hooks/useContractEvents.js
/** https://wagmi.sh/react/api/hooks/useContractEvents */
function useContractEvents(parameters = {}) {
	const config = useConfig(parameters);
	const chainId = useChainId({ config });
	return useQuery(getContractEventsQueryOptions(config, {
		...parameters,
		chainId: parameters.chainId ?? chainId
	}));
}
//#endregion
//#region node_modules/wagmi/dist/esm/hooks/useDeployContract.js
/** https://wagmi.sh/react/api/hooks/useDeployContract */
function useDeployContract(parameters = {}) {
	const mutation = useMutation(deployContractMutationOptions(useConfig(parameters), parameters));
	return {
		...mutation,
		deployContract: mutation.mutate,
		deployContractAsync: mutation.mutateAsync
	};
}
//#endregion
//#region node_modules/wagmi/dist/esm/hooks/useDisconnect.js
/** https://wagmi.sh/react/api/hooks/useDisconnect */
function useDisconnect(parameters = {}) {
	const config = useConfig(parameters);
	const mutation = useMutation(disconnectMutationOptions(config, parameters));
	return {
		...mutation,
		connectors: useConnections({ config }).map((connection) => connection.connector),
		disconnect: mutation.mutate,
		disconnectAsync: mutation.mutateAsync
	};
}
//#endregion
//#region node_modules/wagmi/dist/esm/hooks/useEnsAddress.js
/** https://wagmi.sh/react/api/hooks/useEnsAddress */
function useEnsAddress(parameters = {}) {
	const config = useConfig(parameters);
	const chainId = useChainId({ config });
	return useQuery(getEnsAddressQueryOptions(config, {
		...parameters,
		chainId: parameters.chainId ?? chainId
	}));
}
//#endregion
//#region node_modules/wagmi/dist/esm/hooks/useEnsAvatar.js
/** https://wagmi.sh/react/api/hooks/useEnsAvatar */
function useEnsAvatar(parameters = {}) {
	const config = useConfig(parameters);
	const chainId = useChainId({ config });
	return useQuery(getEnsAvatarQueryOptions(config, {
		...parameters,
		chainId: parameters.chainId ?? chainId
	}));
}
//#endregion
//#region node_modules/wagmi/dist/esm/hooks/useEnsName.js
/** https://wagmi.sh/react/api/hooks/useEnsName */
function useEnsName(parameters = {}) {
	const config = useConfig(parameters);
	const chainId = useChainId({ config });
	return useQuery(getEnsNameQueryOptions(config, {
		...parameters,
		chainId: parameters.chainId ?? chainId
	}));
}
//#endregion
//#region node_modules/wagmi/dist/esm/hooks/useEnsResolver.js
/** https://wagmi.sh/react/api/hooks/useEnsResolver */
function useEnsResolver(parameters = {}) {
	const config = useConfig(parameters);
	const chainId = useChainId({ config });
	return useQuery(getEnsResolverQueryOptions(config, {
		...parameters,
		chainId: parameters.chainId ?? chainId
	}));
}
//#endregion
//#region node_modules/wagmi/dist/esm/hooks/useEnsText.js
/** https://wagmi.sh/react/api/hooks/useEnsText */
function useEnsText(parameters = {}) {
	const config = useConfig(parameters);
	const chainId = useChainId({ config });
	return useQuery(getEnsTextQueryOptions(config, {
		...parameters,
		chainId: parameters.chainId ?? chainId
	}));
}
//#endregion
//#region node_modules/wagmi/dist/esm/hooks/useEstimateFeesPerGas.js
/** https://wagmi.sh/react/api/hooks/useEstimateFeesPerGas */
function useEstimateFeesPerGas(parameters = {}) {
	const config = useConfig(parameters);
	const chainId = useChainId({ config });
	return useQuery(estimateFeesPerGasQueryOptions(config, {
		...parameters,
		chainId: parameters.chainId ?? chainId
	}));
}
//#endregion
//#region node_modules/wagmi/dist/esm/hooks/useEstimateGas.js
function useEstimateGas(parameters = {}) {
	const config = useConfig(parameters);
	const { address, connector } = useConnection();
	const chainId = useChainId({ config });
	return useQuery(estimateGasQueryOptions(config, {
		...parameters,
		account: parameters.account ?? address,
		chainId: parameters.chainId ?? chainId,
		connector: parameters.connector ?? connector
	}));
}
//#endregion
//#region node_modules/wagmi/dist/esm/hooks/useEstimateMaxPriorityFeePerGas.js
/** https://wagmi.sh/react/api/hooks/useEstimateMaxPriorityFeePerGas */
function useEstimateMaxPriorityFeePerGas(parameters = {}) {
	const config = useConfig(parameters);
	const chainId = useChainId({ config });
	return useQuery(estimateMaxPriorityFeePerGasQueryOptions(config, {
		...parameters,
		chainId: parameters.chainId ?? chainId
	}));
}
//#endregion
//#region node_modules/wagmi/dist/esm/hooks/useFeeHistory.js
/** https://wagmi.sh/react/api/hooks/useFeeHistory */
function useFeeHistory(parameters = {}) {
	const config = useConfig(parameters);
	const chainId = useChainId({ config });
	return useQuery(getFeeHistoryQueryOptions(config, {
		...parameters,
		chainId: parameters.chainId ?? chainId
	}));
}
//#endregion
//#region node_modules/wagmi/dist/esm/hooks/useGasPrice.js
/** https://wagmi.sh/react/api/hooks/useGasPrice */
function useGasPrice(parameters = {}) {
	const config = useConfig(parameters);
	const chainId = useChainId({ config });
	return useQuery(getGasPriceQueryOptions(config, {
		...parameters,
		chainId: parameters.chainId ?? chainId
	}));
}
//#endregion
//#region node_modules/wagmi/dist/esm/hooks/useInfiniteReadContracts.js
/** https://wagmi.sh/react/api/hooks/useInfiniteReadContracts */
function useInfiniteReadContracts(parameters) {
	const { contracts = [], query } = parameters;
	const config = useConfig(parameters);
	const chainId = useChainId({ config });
	const options = infiniteReadContractsQueryOptions(config, {
		...parameters,
		chainId,
		contracts,
		query
	});
	return useInfiniteQuery({
		...query,
		...options,
		initialPageParam: options.initialPageParam,
		structuralSharing: query.structuralSharing ?? structuralSharing
	});
}
//#endregion
//#region node_modules/wagmi/dist/esm/hooks/usePrepareTransactionRequest.js
/** https://wagmi.sh/react/api/hooks/usePrepareTransactionRequest */
function usePrepareTransactionRequest(parameters = {}) {
	const config = useConfig(parameters);
	const chainId = useChainId({ config });
	return useQuery(prepareTransactionRequestQueryOptions(config, {
		...parameters,
		chainId: parameters.chainId ?? chainId
	}));
}
//#endregion
//#region node_modules/wagmi/dist/esm/hooks/useProof.js
/** https://wagmi.sh/react/api/hooks/useProof */
function useProof(parameters = {}) {
	const config = useConfig(parameters);
	const chainId = useChainId({ config });
	return useQuery(getProofQueryOptions(config, {
		...parameters,
		chainId: parameters.chainId ?? chainId
	}));
}
//#endregion
//#region node_modules/wagmi/dist/esm/hooks/usePublicClient.js
/** https://wagmi.sh/react/api/hooks/usePublicClient */
function usePublicClient(parameters = {}) {
	const config = useConfig(parameters);
	return (0, import_with_selector.useSyncExternalStoreWithSelector)((onChange) => watchPublicClient(config, { onChange }), () => getPublicClient(config, parameters), () => getPublicClient(config, parameters), (x) => x, (a, b) => a?.uid === b?.uid);
}
//#endregion
//#region node_modules/wagmi/dist/esm/hooks/useReadContract.js
/** https://wagmi.sh/react/api/hooks/useReadContract */
function useReadContract(parameters = {}) {
	const config = useConfig(parameters);
	const chainId = useChainId({ config });
	return useQuery(readContractQueryOptions(config, {
		...parameters,
		chainId: parameters.chainId ?? chainId
	}));
}
//#endregion
//#region node_modules/wagmi/dist/esm/hooks/useReadContracts.js
/** https://wagmi.sh/react/api/hooks/useReadContracts */
function useReadContracts(parameters = {}) {
	const config = useConfig(parameters);
	const chainId = useChainId({ config });
	const contractsChainId = (0, import_react.useMemo)(() => {
		const firstChainId = parameters.contracts?.[0]?.chainId;
		if ((parameters.contracts ?? []).every((contract) => contract.chainId === firstChainId)) return firstChainId;
	}, [parameters.contracts]);
	return useQuery(readContractsQueryOptions(config, {
		...parameters,
		chainId: parameters.chainId ?? contractsChainId ?? chainId
	}));
}
//#endregion
//#region node_modules/wagmi/dist/esm/hooks/useReconnect.js
/** https://wagmi.sh/react/api/hooks/useReconnect */
function useReconnect(parameters = {}) {
	const config = useConfig(parameters);
	const mutation = useMutation(reconnectMutationOptions(config, parameters));
	return {
		...mutation,
		connectors: config.connectors,
		reconnect: mutation.mutate,
		reconnectAsync: mutation.mutateAsync
	};
}
//#endregion
//#region node_modules/wagmi/dist/esm/hooks/useSendCalls.js
/** https://wagmi.sh/react/api/hooks/useSendCalls */
function useSendCalls(parameters = {}) {
	const mutation = useMutation(sendCallsMutationOptions(useConfig(parameters), parameters));
	return {
		...mutation,
		sendCalls: mutation.mutate,
		sendCallsAsync: mutation.mutateAsync
	};
}
//#endregion
//#region node_modules/wagmi/dist/esm/hooks/useSendCallsSync.js
/** https://wagmi.sh/react/api/hooks/useSendCallsSync */
function useSendCallsSync(parameters = {}) {
	const mutation = useMutation(sendCallsSyncMutationOptions(useConfig(parameters), parameters));
	return {
		...mutation,
		sendCallsSync: mutation.mutate,
		sendCallsSyncAsync: mutation.mutateAsync
	};
}
//#endregion
//#region node_modules/wagmi/dist/esm/hooks/useSendTransaction.js
/** https://wagmi.sh/react/api/hooks/useSendTransaction */
function useSendTransaction(parameters = {}) {
	const mutation = useMutation(sendTransactionMutationOptions(useConfig(parameters), parameters));
	return {
		...mutation,
		sendTransaction: mutation.mutate,
		sendTransactionAsync: mutation.mutateAsync
	};
}
//#endregion
//#region node_modules/wagmi/dist/esm/hooks/useSendTransactionSync.js
/** https://wagmi.sh/react/api/hooks/useSendTransactionSync */
function useSendTransactionSync(parameters = {}) {
	const mutation = useMutation(sendTransactionSyncMutationOptions(useConfig(parameters), parameters));
	return {
		...mutation,
		sendTransactionSync: mutation.mutate,
		sendTransactionSyncAsync: mutation.mutateAsync
	};
}
//#endregion
//#region node_modules/wagmi/dist/esm/hooks/useShowCallsStatus.js
/** https://wagmi.sh/react/api/hooks/useShowCallsStatus */
function useShowCallsStatus(parameters = {}) {
	const mutation = useMutation(showCallsStatusMutationOptions(useConfig(parameters), parameters));
	return {
		...mutation,
		showCallsStatus: mutation.mutate,
		showCallsStatusAsync: mutation.mutateAsync
	};
}
//#endregion
//#region node_modules/wagmi/dist/esm/hooks/useSignMessage.js
/** https://wagmi.sh/react/api/hooks/useSignMessage */
function useSignMessage(parameters = {}) {
	const mutation = useMutation(signMessageMutationOptions(useConfig(parameters), parameters));
	return {
		...mutation,
		signMessage: mutation.mutate,
		signMessageAsync: mutation.mutateAsync
	};
}
//#endregion
//#region node_modules/wagmi/dist/esm/hooks/useSignTransaction.js
/** https://wagmi.sh/react/api/hooks/useSignTransaction */
function useSignTransaction(parameters = {}) {
	const mutation = useMutation(signTransactionMutationOptions(useConfig(parameters), parameters));
	return {
		...mutation,
		signTransaction: mutation.mutate,
		signTransactionAsync: mutation.mutateAsync
	};
}
//#endregion
//#region node_modules/wagmi/dist/esm/hooks/useSignTypedData.js
/** https://wagmi.sh/react/api/hooks/useSignTypedData */
function useSignTypedData(parameters = {}) {
	const mutation = useMutation(signTypedDataMutationOptions(useConfig(parameters), parameters));
	return {
		...mutation,
		signTypedData: mutation.mutate,
		signTypedDataAsync: mutation.mutateAsync
	};
}
//#endregion
//#region node_modules/wagmi/dist/esm/hooks/useSimulateContract.js
/** https://wagmi.sh/react/api/hooks/useSimulateContract */
function useSimulateContract(parameters = {}) {
	const config = useConfig(parameters);
	const { address, connector } = useConnection();
	const chainId = useChainId({ config });
	return useQuery(simulateContractQueryOptions(config, {
		...parameters,
		account: parameters.account ?? address,
		chainId: parameters.chainId ?? chainId,
		connector: parameters.connector ?? connector
	}));
}
//#endregion
//#region node_modules/wagmi/dist/esm/hooks/useStorageAt.js
/** https://wagmi.sh/react/api/hooks/useStorageAt */
function useStorageAt(parameters = {}) {
	const config = useConfig(parameters);
	const chainId = useChainId({ config });
	return useQuery(getStorageAtQueryOptions(config, {
		...parameters,
		chainId: parameters.chainId ?? chainId
	}));
}
//#endregion
//#region node_modules/wagmi/dist/esm/hooks/useSwitchChain.js
/** https://wagmi.sh/react/api/hooks/useSwitchChain */
function useSwitchChain(parameters = {}) {
	const config = useConfig(parameters);
	const mutation = useMutation(switchChainMutationOptions(config, parameters));
	return {
		...mutation,
		chains: useChains({ config }),
		switchChain: mutation.mutate,
		switchChainAsync: mutation.mutateAsync
	};
}
//#endregion
//#region node_modules/wagmi/dist/esm/hooks/useSwitchConnection.js
/** https://wagmi.sh/react/api/hooks/useSwitchConnection */
function useSwitchConnection(parameters = {}) {
	const config = useConfig(parameters);
	const mutation = useMutation(switchConnectionMutationOptions(config, parameters));
	return {
		...mutation,
		connectors: useConnections({ config }).map((connection) => connection.connector),
		switchAccount: mutation.mutate,
		switchAccountAsync: mutation.mutateAsync,
		switchConnection: mutation.mutate,
		switchConnectionAsync: mutation.mutateAsync
	};
}
//#endregion
//#region node_modules/wagmi/dist/esm/hooks/useTransaction.js
/** https://wagmi.sh/react/api/hooks/useTransaction */
function useTransaction(parameters = {}) {
	const config = useConfig(parameters);
	const chainId = useChainId({ config });
	return useQuery(getTransactionQueryOptions(config, {
		...parameters,
		chainId: parameters.chainId ?? chainId
	}));
}
//#endregion
//#region node_modules/wagmi/dist/esm/hooks/useTransactionConfirmations.js
/** https://wagmi.sh/react/api/hooks/useTransactionConfirmations */
function useTransactionConfirmations(parameters = {}) {
	const config = useConfig(parameters);
	const chainId = useChainId({ config });
	return useQuery(getTransactionConfirmationsQueryOptions(config, {
		...parameters,
		chainId: parameters.chainId ?? chainId
	}));
}
//#endregion
//#region node_modules/wagmi/dist/esm/hooks/useTransactionCount.js
/** https://wagmi.sh/react/api/hooks/useTransactionCount */
function useTransactionCount(parameters = {}) {
	const config = useConfig(parameters);
	const chainId = useChainId({ config });
	return useQuery(getTransactionCountQueryOptions(config, {
		...parameters,
		chainId: parameters.chainId ?? chainId
	}));
}
//#endregion
//#region node_modules/wagmi/dist/esm/hooks/useTransactionReceipt.js
/** https://wagmi.sh/react/api/hooks/useTransactionReceipt */
function useTransactionReceipt(parameters = {}) {
	const config = useConfig(parameters);
	const chainId = useChainId({ config });
	return useQuery(getTransactionReceiptQueryOptions(config, {
		...parameters,
		chainId: parameters.chainId ?? chainId
	}));
}
//#endregion
//#region node_modules/wagmi/dist/esm/hooks/useVerifyMessage.js
/** https://wagmi.sh/react/api/hooks/useVerifyMessage */
function useVerifyMessage(parameters = {}) {
	const config = useConfig(parameters);
	const chainId = useChainId({ config });
	return useQuery(verifyMessageQueryOptions(config, {
		...parameters,
		chainId: parameters.chainId ?? chainId
	}));
}
//#endregion
//#region node_modules/wagmi/dist/esm/hooks/useVerifyTypedData.js
/** https://wagmi.sh/react/api/hooks/useVerifyTypedData */
function useVerifyTypedData(parameters = {}) {
	const config = useConfig(parameters);
	const chainId = useChainId({ config });
	return useQuery(verifyTypedDataQueryOptions(config, {
		...parameters,
		chainId: parameters.chainId ?? chainId
	}));
}
//#endregion
//#region node_modules/wagmi/dist/esm/hooks/useWaitForCallsStatus.js
/** https://wagmi.sh/react/api/hooks/useWaitForCallsStatus */
function useWaitForCallsStatus(parameters) {
	const config = useConfig(parameters);
	const { connector } = useConnection({ config });
	return useQuery(waitForCallsStatusQueryOptions(config, {
		...parameters,
		connector: parameters.connector ?? connector
	}));
}
//#endregion
//#region node_modules/wagmi/dist/esm/hooks/useWaitForTransactionReceipt.js
/** https://wagmi.sh/react/api/hooks/useWaitForTransactionReceipt */
function useWaitForTransactionReceipt(parameters = {}) {
	const config = useConfig(parameters);
	const chainId = useChainId({ config });
	return useQuery(waitForTransactionReceiptQueryOptions(config, {
		...parameters,
		chainId: parameters.chainId ?? chainId
	}));
}
//#endregion
//#region node_modules/wagmi/dist/esm/hooks/useWalletClient.js
/** https://wagmi.sh/react/api/hooks/useWalletClient */
function useWalletClient(parameters = {}) {
	const config = useConfig(parameters);
	const chainId = useChainId({ config });
	const { address, connector } = useConnection({ config });
	const options = getWalletClientQueryOptions(config, {
		...parameters,
		chainId: parameters.chainId ?? chainId,
		connector: parameters.connector ?? connector,
		query: parameters.query
	});
	const addressRef = (0, import_react.useRef)(address);
	const queryClient = useQueryClient();
	(0, import_react.useEffect)(() => {
		const previousAddress = addressRef.current;
		if (!address && previousAddress) {
			queryClient.removeQueries({ queryKey: options.queryKey });
			addressRef.current = void 0;
		} else if (address !== previousAddress) {
			queryClient.invalidateQueries({ queryKey: options.queryKey });
			addressRef.current = address;
		}
	}, [address, queryClient]);
	return useQuery(options);
}
//#endregion
//#region node_modules/wagmi/dist/esm/hooks/useWatchAsset.js
/** https://wagmi.sh/react/api/hooks/useWatchAsset */
function useWatchAsset(parameters = {}) {
	const mutation = useMutation(watchAssetMutationOptions(useConfig(parameters), parameters));
	return {
		...mutation,
		watchAsset: mutation.mutate,
		watchAssetAsync: mutation.mutateAsync
	};
}
//#endregion
//#region node_modules/wagmi/dist/esm/hooks/useWatchContractEvent.js
/** https://wagmi.sh/react/api/hooks/useWatchContractEvent */
function useWatchContractEvent(parameters = {}) {
	const { enabled = true, onLogs, config: _, ...rest } = parameters;
	const config = useConfig(parameters);
	const configChainId = useChainId({ config });
	const chainId = parameters.chainId ?? configChainId;
	const onLogsRef = (0, import_react.useRef)(onLogs);
	const onErrorRef = (0, import_react.useRef)(rest.onError);
	onLogsRef.current = onLogs;
	onErrorRef.current = rest.onError;
	const abiRef = (0, import_react.useRef)(rest.abi);
	const addressRef = (0, import_react.useRef)(rest.address);
	const argsRef = (0, import_react.useRef)(rest.args);
	if (!abiRef.current || !deepEqual(abiRef.current, rest.abi)) abiRef.current = rest.abi;
	if (!addressRef.current || !deepEqual(addressRef.current, rest.address)) addressRef.current = rest.address;
	if (!argsRef.current || !deepEqual(argsRef.current, rest.args)) argsRef.current = rest.args;
	(0, import_react.useEffect)(() => {
		if (!enabled) return;
		if (!onLogsRef.current) return;
		return watchContractEvent(config, {
			...rest,
			chainId,
			onLogs: (logs) => onLogsRef.current?.(logs),
			onError: (error) => onErrorRef.current?.(error)
		});
	}, [
		chainId,
		config,
		enabled,
		abiRef.current,
		addressRef.current,
		argsRef.current,
		rest.batch,
		rest.eventName,
		rest.fromBlock,
		rest.poll,
		rest.pollingInterval,
		rest.strict,
		rest.syncConnectedChain
	]);
}
//#endregion
//#region node_modules/wagmi/dist/esm/hooks/useWatchPendingTransactions.js
/** https://wagmi.sh/react/api/hooks/useWatchPendingTransactions */
function useWatchPendingTransactions(parameters = {}) {
	const { enabled = true, onTransactions, config: _, ...rest } = parameters;
	const config = useConfig(parameters);
	const configChainId = useChainId({ config });
	const chainId = parameters.chainId ?? configChainId;
	const onTransactionsRef = (0, import_react.useRef)(onTransactions);
	const onErrorRef = (0, import_react.useRef)(rest.onError);
	onTransactionsRef.current = onTransactions;
	onErrorRef.current = rest.onError;
	(0, import_react.useEffect)(() => {
		if (!enabled) return;
		if (!onTransactionsRef.current) return;
		return watchPendingTransactions(config, {
			...rest,
			chainId,
			onTransactions: (transactions) => onTransactionsRef.current?.(transactions),
			onError: (error) => onErrorRef.current?.(error)
		});
	}, [
		chainId,
		config,
		enabled,
		rest.batch,
		rest.poll,
		rest.pollingInterval,
		rest.syncConnectedChain
	]);
}
//#endregion
//#region node_modules/wagmi/dist/esm/hooks/useWriteContract.js
/** https://wagmi.sh/react/api/hooks/useWriteContract */
function useWriteContract(parameters = {}) {
	const mutation = useMutation(writeContractMutationOptions(useConfig(parameters), parameters));
	return {
		...mutation,
		writeContract: mutation.mutate,
		writeContractAsync: mutation.mutateAsync
	};
}
//#endregion
//#region node_modules/wagmi/dist/esm/hooks/useWriteContractSync.js
/** https://wagmi.sh/react/api/hooks/useWriteContractSync */
function useWriteContractSync(parameters = {}) {
	const mutation = useMutation(writeContractSyncMutationOptions(useConfig(parameters), parameters));
	return {
		...mutation,
		writeContractSync: mutation.mutate,
		writeContractSyncAsync: mutation.mutateAsync
	};
}
//#endregion
export { BaseError, ChainNotConfiguredError, ConnectorAccountNotFoundError, ConnectorAlreadyConnectedError, ConnectorChainMismatchError, ConnectorNotFoundError, ConnectorUnavailableReconnectingError, Hydrate, ProviderNotFoundError, SwitchChainNotSupportedError, WagmiContext, WagmiProvider, WagmiProviderNotFoundError, cookieStorage, cookieToInitialState, createConfig, createConnector, createStorage, custom, deepEqual, deserialize, fallback, http, injected, mock, noopStorage, parseCookie, serialize, unstable_connector, useConnection as useAccount, useConnection, useConnectionEffect as useAccountEffect, useConnectionEffect, useBalance, useBlobBaseFee, useBlock, useBlockNumber, useBlockTransactionCount, useBytecode, useCall, useCallsStatus, useCapabilities, useChainId, useChains, useClient, useConfig, useConnect, useConnections, useConnectorClient, useConnectors, useContractEvents, useDeployContract, useDisconnect, useEnsAddress, useEnsAvatar, useEnsName, useEnsResolver, useEnsText, useEstimateFeesPerGas, useEstimateGas, useEstimateMaxPriorityFeePerGas, useFeeHistory, useGasPrice, useInfiniteReadContracts, usePrepareTransactionRequest, useProof, usePublicClient, useReadContract, useReadContracts, useReconnect, useSendCalls, useSendCallsSync, useSendTransaction, useSendTransactionSync, useShowCallsStatus, useSignMessage, useSignTransaction, useSignTypedData, useSimulateContract, useStorageAt, useSwitchConnection as useSwitchAccount, useSwitchConnection, useSwitchChain, useTransaction, useTransactionConfirmations, useTransactionCount, useTransactionReceipt, useVerifyMessage, useVerifyTypedData, useWaitForCallsStatus, useWaitForTransactionReceipt, useWalletClient, useWatchAsset, useWatchBlockNumber, useWatchBlocks, useWatchContractEvent, useWatchPendingTransactions, useWriteContract, useWriteContractSync, version, webSocket };

//# sourceMappingURL=wagmi.js.map
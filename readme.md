# USD8 Docs
This docs site is a React/Vite app that builds static files into `docs/` for GitHub Pages.

`npm install` installs dependencies.
`npm run dev` starts the local React dev server.
`npm run build` builds the static site into `docs/`.
`npm run deploy` builds, commits changed files, and pushes the current branch.

{
  "scripts": {
    "dev": "node scripts/dev.mjs",
    "build": "npm run build:app && mdbook build",
    "test": "NODE_ENV=test vitest run --configLoader native",
    "preview": "vite preview --configLoader native --host 0.0.0.0",
    "deploy": "./deploy.sh",
    "docs:serve": "mdbook serve --open",
    "build:app": "vite build --configLoader native && node scripts/postbuild.mjs",
    "check:bundle": "node scripts/check-bundle.mjs",
    "check:wallet": "node scripts/check-wallet-dependencies.mjs"
  },
  "dependencies": {
    "@reown/appkit": "^1.8.20",
    "@reown/appkit-adapter-wagmi": "^1.8.20",
    "@tanstack/react-query": "^5.100.14",
    "lucide-react": "^1.17.0",
    "react": "^19.2.6",
    "react-dom": "^19.2.6",
    "viem": "2.54.6",
    "wagmi": "^3.6.16"
  },
  "devDependencies": {
    "@testing-library/jest-dom": "^7.0.0",
    "@testing-library/react": "^16.3.2",
    "@vitejs/plugin-react": "^6.0.2",
    "jsdom": "^30.0.1",
    "vite": "^8.0.14",
    "vitest": "^4.1.10"
  }
}
